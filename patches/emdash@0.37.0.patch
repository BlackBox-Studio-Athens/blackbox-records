diff --git a/dist/api-Dz6rgoa3.mjs b/dist/api-Dz6rgoa3.mjs
index b538412fc8ebb5b5d44aee3c558347f721a70047..dfce6d399d6153082eac80871faadbb28f3ecdd1 100644
--- a/dist/api-Dz6rgoa3.mjs
+++ b/dist/api-Dz6rgoa3.mjs
@@ -917,21 +917,23 @@ async function handleContentDuplicate(db, collection, id, authorId) {
 /**
 * Delete content item (soft delete - moves to trash)
 */
-async function handleContentDelete(db, collection, id) {
+async function handleContentDelete(db, collection, id, rev) {
+	const expected = rev === undefined ? undefined : decodeRev(rev);
+	if (rev !== undefined && !expected) return { success: false, error: { code: "CONFLICT", message: "Malformed _rev token" } };
 	try {
 		const result = await withTransaction(db, async (trx) => {
 			const repo = new ContentRepository(trx);
 			const resolvedId = await resolveId(repo, collection, id) ?? id;
 			return {
 				id: resolvedId,
-				deleted: await repo.delete(collection, resolvedId)
+				deleted: await repo.delete(collection, resolvedId, expected ?? undefined)
 			};
 		});
 		if (!result.deleted) return {
 			success: false,
 			error: {
-				code: "NOT_FOUND",
-				message: `Content item not found: ${id}`
+				code: expected ? "CONFLICT" : "NOT_FOUND",
+				message: expected ? "Content has been modified since last read (version conflict)" : `Content item not found: ${id}`
 			}
 		};
 		return {
diff --git a/dist/astro/middleware.mjs b/dist/astro/middleware.mjs
index 68e9c0310f20279650d4a2a8b91ac34c9afa25fe..3674d12e53edbe7550b86d591e3d2b143f6d9f77 100644
--- a/dist/astro/middleware.mjs
+++ b/dist/astro/middleware.mjs
@@ -2177,7 +2177,7 @@ var EmDashRuntime = class EmDashRuntime {
 			if ((await this.schemaRegistry.getCollectionWithFields(collection))?.supports?.includes("revisions")) {
 				usesDraftRevisions = true;
 				const revisionRepo = new RevisionRepository(this.db);
-				let existing = await repo.findById(collection, resolvedId);
+				let existing = body._rev ? resolvedItem : await repo.findById(collection, resolvedId);
 				for (let attempt = 0; existing && attempt < MAX_DRAFT_STAGE_ATTEMPTS; attempt++) {
 					let baseData;
 					if (existing.draftRevisionId) baseData = (await revisionRepo.findById(existing.draftRevisionId))?.data ?? existing.data;
@@ -2272,7 +2272,7 @@ var EmDashRuntime = class EmDashRuntime {
 		};
 		return hydrated;
 	}
-	async handleContentDelete(collection, id) {
+	async handleContentDelete(collection, id, rev) {
 		if (this.hooks.hasHooks("content:beforeDelete")) {
 			const { allowed } = await this.hooks.runContentBeforeDelete(id, collection);
 			if (!allowed) return {
@@ -2290,7 +2290,7 @@ var EmDashRuntime = class EmDashRuntime {
 				message: "Delete blocked by sandboxed plugin hook"
 			}
 		};
-		const result = await handleContentDelete(this.db, collection, id);
+		const result = await handleContentDelete(this.db, collection, id, rev);
 		if (result.success) await this.refreshContentUsageAfterSuccessfulWrite(collection, [result.data.id]);
 		if (result.success) this.runAfterDeleteHooks(id, collection, false);
 		return result;
diff --git a/dist/astro/routes/api/content/_collection_/_id_.mjs b/dist/astro/routes/api/content/_collection_/_id_.mjs
index fd9072f89df5ef4d150effe42577581f1f5324ed..2acd650ad11b34f5938f06b4894f50fe4836134d 100644
--- a/dist/astro/routes/api/content/_collection_/_id_.mjs
+++ b/dist/astro/routes/api/content/_collection_/_id_.mjs
@@ -3,7 +3,7 @@ import "../../../../../base64-CvLhUKQv.mjs";
 import "../../../../../types-CQPWhPxE.mjs";
 import { n as mapErrorStatus } from "../../../../../errors-DsVoDUjJ.mjs";
 import { a as unwrapResult, t as apiError } from "../../../../../error-DFrQjppn.mjs";
-import { n as parseBody, t as isParseError } from "../../../../../parse-Ckpg-Qfp.mjs";
+import { n as parseBody, r as parseOptionalBody, t as isParseError } from "../../../../../parse-Ckpg-Qfp.mjs";
 import "../../../../../media-BHjus86Y.mjs";
 import { fn as contentUpdateBody } from "../../../../../redirects-BLNOWQTn.mjs";
 import "../../../../../relations-C4htk5sb.mjs";
@@ -71,7 +71,9 @@ const PUT = async ({ params, request, locals, cache }) => {
 	if (cache?.enabled && result.liveContentChanged !== false) await cache.invalidate({ tags: [collection, resolvedId] });
 	return unwrapResult(result);
 };
-const DELETE = async ({ params, locals, url, cache }) => {
+const DELETE = async ({ params, request, locals, url, cache }) => {
+	const body = await parseOptionalBody(request, contentUpdateBody, {});
+	if (isParseError(body)) return body;
 	const { emdash, user } = locals;
 	const collection = params.collection;
 	const id = params.id;
@@ -84,7 +86,7 @@ const DELETE = async ({ params, locals, url, cache }) => {
 	const deleteDenied = requireOwnerPerm(user, typeof deleteItem?.authorId === "string" ? deleteItem.authorId : "", "content:delete_own", "content:delete_any");
 	if (deleteDenied) return deleteDenied;
 	const resolvedId = typeof deleteItem?.id === "string" ? deleteItem.id : id;
-	const result = await emdash.handleContentDelete(collection, resolvedId);
+	const result = await emdash.handleContentDelete(collection, resolvedId, body._rev);
 	if (!result.success) return unwrapResult(result);
 	if (cache?.enabled) await cache.invalidate({ tags: [collection, resolvedId] });
 	return unwrapResult(result);
diff --git a/dist/content-DjvM0T92.mjs b/dist/content-DjvM0T92.mjs
index 9361026625422d14c2272a20ee7bf96d309a9cf6..a3bdf26c945246c15ecece6f1ae6f700c530f9ea 100644
--- a/dist/content-DjvM0T92.mjs
+++ b/dist/content-DjvM0T92.mjs
@@ -809,14 +809,15 @@ var ContentRepository = class {
 	/**
 	* Delete content (soft delete - moves to trash)
 	*/
-	async delete(type, id) {
+	async delete(type, id, expected) {
 		const tableName = getTableName(type);
 		const now = (/* @__PURE__ */ new Date()).toISOString();
 		const changed = ((await sql`
 			UPDATE ${sql.ref(tableName)}
-			SET deleted_at = ${now}
+			SET deleted_at = ${now}, version = version + 1, updated_at = ${now}
 			WHERE id = ${id}
 			AND deleted_at IS NULL
+			AND (${expected ? sql`version = ${expected.version} AND updated_at = ${expected.updatedAt}` : sql`1 = 1`})
 		`.execute(this.db)).numAffectedRows ?? 0n) > 0n;
 		if (changed) invalidateCollectionCache(type);
 		return changed;
diff --git a/dist/index-BJNMCWmu.d.mts b/dist/index-BJNMCWmu.d.mts
index 2ba1e11b80398725b2fbf3ed94c47f72ff91e2d3..ed61c9199f9314dda8d7434457052d7ce995ecb1 100644
--- a/dist/index-BJNMCWmu.d.mts
+++ b/dist/index-BJNMCWmu.d.mts
@@ -118,7 +118,7 @@ declare class ContentRepository {
   /**
    * Delete content (soft delete - moves to trash)
    */
-  delete(type: string, id: string): Promise<boolean>;
+  delete(type: string, id: string, expected?: ContentRevisionPrecondition): Promise<boolean>;
   /**
    * Restore content from trash
    */
@@ -935,7 +935,7 @@ declare function handleContentDuplicate(db: Kysely<Database>, collection: string
 /**
  * Delete content item (soft delete - moves to trash)
  */
-declare function handleContentDelete(db: Kysely<Database>, collection: string, id: string): Promise<ApiResult<{
+declare function handleContentDelete(db: Kysely<Database>, collection: string, id: string, rev?: string): Promise<ApiResult<{
   deleted: true;
   id: string;
 }>>;
@@ -3639,7 +3639,7 @@ declare class EmDashRuntime {
     success: true;
     data: ContentResponse;
   }>;
-  handleContentDelete(collection: string, id: string): Promise<{
+  handleContentDelete(collection: string, id: string, rev?: string): Promise<{
     success: false;
     error: {
       code: string;
diff --git a/src/api/handlers/content.ts b/src/api/handlers/content.ts
index fef207ff755bd3939bba716da9825de3cde5a229..87ee336c2340fe348db10ef97e40fa5e62e51056 100644
--- a/src/api/handlers/content.ts
+++ b/src/api/handlers/content.ts
@@ -1278,14 +1278,17 @@ export async function handleContentDelete(
 	db: Kysely<Database>,
 	collection: string,
 	id: string,
+	rev?: string,
 ): Promise<ApiResult<{ deleted: true; id: string }>> {
+	const expected = rev === undefined ? undefined : decodeRev(rev);
+	if (rev !== undefined && !expected) return { success: false, error: { code: "CONFLICT", message: "Malformed _rev token" } };
 	try {
 		const result = await withTransaction(db, async (trx) => {
 			const repo = new ContentRepository(trx);
 			const resolvedId = (await resolveId(repo, collection, id)) ?? id;
 			return {
 				id: resolvedId,
-				deleted: await repo.delete(collection, resolvedId),
+				deleted: await repo.delete(collection, resolvedId, expected ?? undefined),
 			};
 		});
 
@@ -1293,8 +1296,8 @@ export async function handleContentDelete(
 			return {
 				success: false,
 				error: {
-					code: "NOT_FOUND",
-					message: `Content item not found: ${id}`,
+					code: expected ? "CONFLICT" : "NOT_FOUND",
+					message: expected ? "Content has been modified since last read (version conflict)" : `Content item not found: ${id}`,
 				},
 			};
 		}
diff --git a/src/astro/routes/api/content/[collection]/[id].ts b/src/astro/routes/api/content/[collection]/[id].ts
index 2c1ac3c1aa8d7f5c2c4fd867f4fb4bc885e04de8..78ec4351dd56a10eb53d5ad1933eb28014483659 100644
--- a/src/astro/routes/api/content/[collection]/[id].ts
+++ b/src/astro/routes/api/content/[collection]/[id].ts
@@ -11,7 +11,7 @@ import type { APIRoute } from "astro";
 
 import { requirePerm, requireOwnerPerm } from "#api/authorize.js";
 import { apiError, mapErrorStatus, unwrapResult } from "#api/error.js";
-import { parseBody, isParseError } from "#api/parse.js";
+import { parseBody, parseOptionalBody, isParseError } from "#api/parse.js";
 import { contentUpdateBody } from "#api/schemas.js";
 
 export const prerender = false;
@@ -133,7 +133,9 @@ export const PUT: APIRoute = async ({ params, request, locals, cache }) => {
 	return unwrapResult(result);
 };
 
-export const DELETE: APIRoute = async ({ params, locals, url, cache }) => {
+export const DELETE: APIRoute = async ({ params, request, locals, url, cache }) => {
+	const body = await parseOptionalBody(request, contentUpdateBody, {});
+	if (isParseError(body)) return body;
 	const { emdash, user } = locals;
 	const collection = params.collection!;
 	const id = params.id!;
@@ -172,7 +174,7 @@ export const DELETE: APIRoute = async ({ params, locals, url, cache }) => {
 	// Use the resolved ID (handles slug → ID resolution)
 	const resolvedId = typeof deleteItem?.id === "string" ? deleteItem.id : id;
 
-	const result = await emdash.handleContentDelete(collection, resolvedId);
+	const result = await emdash.handleContentDelete(collection, resolvedId, body._rev);
 
 	if (!result.success) return unwrapResult(result);
 
diff --git a/src/database/repositories/content.ts b/src/database/repositories/content.ts
index 2699c2a919852c8d29aa28d9c492060ddd81b995..8a1e67787dc1940485d48b55f1792f60e6c87a2b 100644
--- a/src/database/repositories/content.ts
+++ b/src/database/repositories/content.ts
@@ -1077,15 +1077,16 @@ export class ContentRepository {
 	/**
 	 * Delete content (soft delete - moves to trash)
 	 */
-	async delete(type: string, id: string): Promise<boolean> {
+	async delete(type: string, id: string, expected?: ContentRevisionPrecondition): Promise<boolean> {
 		const tableName = getTableName(type);
 		const now = new Date().toISOString();
 
 		const result = await sql`
 			UPDATE ${sql.ref(tableName)}
-			SET deleted_at = ${now}
+			SET deleted_at = ${now}, version = version + 1, updated_at = ${now}
 			WHERE id = ${id}
 			AND deleted_at IS NULL
+			AND (${expected ? sql`version = ${expected.version} AND updated_at = ${expected.updatedAt}` : sql`1 = 1`})
 		`.execute(this.db);
 
 		const changed = (result.numAffectedRows ?? 0n) > 0n;
diff --git a/src/emdash-runtime.ts b/src/emdash-runtime.ts
index 5a0826babdb224c2de6a4c023b3b1ce1a0ac44bc..2bc71151bc5741d0d6dec37419316568e61b5ebf 100644
--- a/src/emdash-runtime.ts
+++ b/src/emdash-runtime.ts
@@ -2949,7 +2949,7 @@ export class EmDashRuntime {
 			if (collectionInfo?.supports?.includes("revisions")) {
 				usesDraftRevisions = true;
 				const revisionRepo = new RevisionRepository(this.db);
-				let existing = await repo.findById(collection, resolvedId);
+				let existing = body._rev ? resolvedItem : await repo.findById(collection, resolvedId);
 
 				for (let attempt = 0; existing && attempt < MAX_DRAFT_STAGE_ATTEMPTS; attempt++) {
 					let baseData: Record<string, unknown>;
@@ -3116,7 +3116,7 @@ export class EmDashRuntime {
 		return hydrated;
 	}
 
-	async handleContentDelete(collection: string, id: string) {
+	async handleContentDelete(collection: string, id: string, rev?: string) {
 		// Run beforeDelete hooks (trusted plugins)
 		if (this.hooks.hasHooks("content:beforeDelete")) {
 			const { allowed } = await this.hooks.runContentBeforeDelete(id, collection);
@@ -3144,7 +3144,7 @@ export class EmDashRuntime {
 		}
 
 		// Delete the content
-		const result = await handleContentDelete(this.db, collection, id);
+		const result = await handleContentDelete(this.db, collection, id, rev);
 		if (result.success) {
 			await this.refreshContentUsageAfterSuccessfulWrite(collection, [result.data.id]);
 		}
