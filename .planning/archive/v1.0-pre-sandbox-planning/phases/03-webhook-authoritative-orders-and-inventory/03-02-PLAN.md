---
phase: 03-webhook-authoritative-orders-and-inventory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md]
autonomous: true
requirements: [CHKO-03, ORDR-02, SECU-02]
user_setup: []
must_haves:
  truths:
    - "The webhook contract is Checkout-session-centric and uses a small authoritative event set."
    - "The contract explicitly requires signature verification, raw-body handling, duplicate safety, and non-browser authority."
  artifacts:
    - path: ".planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md"
      provides: "Authoritative webhook and idempotency contract"
      contains: "checkout.session.completed"
  key_links:
    - from: ".planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md"
      to: ".planning/phases/02-native-catalog-and-embedded-checkout-slice/02-02-PLAN.md"
      via: "shared Checkout Session identifiers and metadata handoff"
      pattern: "Checkout Session|metadata|return pages are not authoritative"
---

<objective>
Define the authoritative webhook contract for paid and unpaid order transitions, including signature verification, idempotency, and event-handling expectations.

Purpose: Give later sandbox implementation one precise webhook authority model instead of a loose collection of Stripe event assumptions.
Output: A planning-only webhook contract for Stripe Checkout Session events and duplicate-safe processing.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-webhook-authoritative-orders-and-inventory/03-CONTEXT.md
@.planning/phases/03-webhook-authoritative-orders-and-inventory/03-RESEARCH.md
@.planning/phases/02-native-catalog-and-embedded-checkout-slice/02-CONTEXT.md
@.planning/phases/02-native-catalog-and-embedded-checkout-slice/02-02-PLAN.md
</context>

<threat_model>
<trust_boundaries>
- Only Worker server routes may verify webhook signatures and mutate authoritative state.
- Browser pages and client-side redirects must not participate in paid-state authority.
- Duplicate or unordered Stripe events must not corrupt order state or stock.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-03-02-A</threat_id>
  <category>Spoofing</category>
  <component>Webhook endpoint</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Document raw-body signature verification and restrict authoritative transitions to verified Stripe webhook requests only.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-02-B</threat_id>
  <category>Tampering</category>
  <component>Duplicate and unordered event handling</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Plan explicit idempotency markers and state-aware transition handling so duplicate or reordered deliveries do not double-apply paid logic.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write the authoritative webhook event contract</name>
  <files>.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md</files>
  <read_first>.planning/phases/03-webhook-authoritative-orders-and-inventory/03-CONTEXT.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-RESEARCH.md, .planning/phases/02-native-catalog-and-embedded-checkout-slice/02-02-PLAN.md</read_first>
  <action>Create `03-WEBHOOK-CONTRACT.md` as a planning-only contract for the Stripe webhook endpoint. Define the accepted authoritative event set (`checkout.session.completed` when paid, `checkout.session.async_payment_succeeded`, `checkout.session.async_payment_failed`, and `checkout.session.expired`), the raw-body signature verification requirement, expected response behavior, and the rule that return/cancel pages are never payment authority. Include a section describing how the webhook locates the target order using Checkout Session identifiers and metadata from the Phase 2 checkout contract.</action>
  <verify>rg -n "checkout.session.completed|checkout.session.async_payment_succeeded|checkout.session.async_payment_failed|checkout.session.expired|raw body|signature|return pages" .planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md</verify>
  <acceptance_criteria>
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` contains `checkout.session.completed`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` contains `checkout.session.async_payment_succeeded`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` contains `checkout.session.async_payment_failed`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` contains `checkout.session.expired`
  </acceptance_criteria>
  <done>The authoritative Stripe webhook event set is documented with current Checkout semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add duplicate-safe and browser-safe processing rules</name>
  <files>.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md</files>
  <read_first>.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-CONTEXT.md</read_first>
  <action>Extend `03-WEBHOOK-CONTRACT.md` with explicit sections for duplicate delivery handling, event-order assumptions, idempotency marker expectations, and browser prohibitions. State that the implementation must tolerate duplicate and out-of-order events, must not rely on a redirect hit to prove payment success, and must keep the webhook's work bounded to state and inventory mutation rather than shipping-side effects. Include a human review checkpoint section covering authoritative events, idempotency, and exception routing to `needs_review`.</action>
  <verify>rg -n "idempot|duplicate|out-of-order|browser|must not|needs_review|human review" .planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md</verify>
  <acceptance_criteria>
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` contains `idempot`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` contains `duplicate`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` contains `out-of-order`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` contains `browser` and `must not`
  </acceptance_criteria>
  <done>The webhook contract includes duplicate-safe and browser-safe processing rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "checkout.session.completed|checkout.session.async_payment_succeeded|checkout.session.async_payment_failed|checkout.session.expired" .planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` returns expected lines
- [ ] `rg -n "idempot|duplicate|out-of-order|browser|must not|raw body|signature" .planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md` returns expected lines
- [ ] `pnpm check` passes
</verification>

<success_criteria>

- All tasks completed
- The webhook authority model is explicit and minimal
- Duplicate and out-of-order deliveries are accounted for
- Browser pages remain non-authoritative by contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-02-SUMMARY.md`
</output>
