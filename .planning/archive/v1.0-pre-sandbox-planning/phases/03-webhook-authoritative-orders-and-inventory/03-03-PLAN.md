---
phase: 03-webhook-authoritative-orders-and-inventory
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: [.planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md, .planning/BACKLOG.md]
autonomous: true
requirements: [ORDR-03, ORDR-04, OPER-01]
user_setup: []
must_haves:
  truths:
    - "Inventory decrement happens once, and only once, after authoritative paid confirmation."
    - "Operator handling stays Stripe-Dashboard-first and future admin/reservation work is explicitly deferred."
  artifacts:
    - path: ".planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md"
      provides: "Atomic stock decrement and low-volume reconciliation contract"
      contains: "Stripe Dashboard"
    - path: ".planning/BACKLOG.md"
      provides: "Deferred v2 operations and stock-control work"
      contains: "reservation"
  key_links:
    - from: ".planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md"
      to: ".planning/BACKLOG.md"
      via: "explicit future deferral of admin tooling and reservation complexity"
      pattern: "Stripe Dashboard|reservation|admin"
---

<objective>
Define how authoritative paid events decrement inventory, how unpaid flows leave stock unchanged, and how operators handle the rare exception path without adding internal tooling.

Purpose: Lock the stock-mutation and reconciliation rules before any sandbox implementation work touches real inventory semantics.
Output: A planning-only inventory and reconciliation contract plus backlog entries for deferred ops complexity.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/BACKLOG.md
@.planning/phases/03-webhook-authoritative-orders-and-inventory/03-CONTEXT.md
@.planning/phases/03-webhook-authoritative-orders-and-inventory/03-RESEARCH.md
@.planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md
@.planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md
</context>

<threat_model>
<trust_boundaries>
- Inventory changes must occur only from verified server-side paid transitions.
- Unpaid, expired, or failed flows must never decrement stock.
- Operator tooling must remain as small as the user approved for MVP.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-03-03-A</threat_id>
  <category>Tampering</category>
  <component>Inventory mutation path</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Document an atomic decrement rule that only executes on the authoritative paid transition and routes conflicts into `needs_review`.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-03-B</threat_id>
  <category>Repudiation</category>
  <component>Operator exception handling</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Define a simple Stripe-Dashboard-first reconciliation procedure and explicitly defer admin and reservation complexity to backlog.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write the inventory decrement and reconciliation contract</name>
  <files>.planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md</files>
  <read_first>.planning/phases/03-webhook-authoritative-orders-and-inventory/03-RESEARCH.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-WEBHOOK-CONTRACT.md</read_first>
  <action>Create `03-INVENTORY-RECONCILIATION.md` as a planning-only contract for stock mutation and operator handling. Define the atomic decrement rule for the first authoritative paid transition, the rule that failed/expired/unpaid flows leave stock untouched, and the low-volume oversell path where a late paid conflict routes to `needs_review` and manual refund handling in Stripe Dashboard. Include a bounded reconciliation playbook describing what the operator checks in Stripe and what minimal D1 facts must exist to support that manual workflow.</action>
  <verify>rg -n "decrement|paid|closed_unpaid|needs_review|Stripe Dashboard|refund|atomic|stock untouched" .planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md</verify>
  <acceptance_criteria>
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md` contains `atomic`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md` contains `Stripe Dashboard`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md` contains `needs_review`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md` contains unpaid or expired flows in a no-decrement context
  </acceptance_criteria>
  <done>The inventory and reconciliation rules are explicit and aligned to low-volume manual ops.</done>
</task>

<task type="auto">
  <name>Task 2: Record deferred v2 ops complexity in the backlog</name>
  <files>.planning/BACKLOG.md</files>
  <read_first>.planning/BACKLOG.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-CONTEXT.md</read_first>
  <action>Update `BACKLOG.md` so the Phase 3 deferrals are explicit: reservation logic, conservative stock buffering, and any internal operator/admin tooling beyond Stripe Dashboard. Tie each deferred item back to the user-approved MVP rule that manual handling is acceptable at low launch volume. Do not move these items into the active milestone; keep them clearly parked for later milestones.</action>
  <verify>rg -n "reservation|buffer|admin|Stripe Dashboard|operator" .planning/BACKLOG.md</verify>
  <acceptance_criteria>
    - `.planning/BACKLOG.md` contains `reservation`
    - `.planning/BACKLOG.md` contains `buffer` or equivalent conservative-stock wording
    - `.planning/BACKLOG.md` contains `admin` or operator-tooling deferral language
    - `.planning/BACKLOG.md` preserves Stripe Dashboard as the MVP operator posture rather than implying new UI is required now
  </acceptance_criteria>
  <done>Deferred operations complexity is parked in the backlog instead of leaking into the MVP phase.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "atomic|paid|needs_review|Stripe Dashboard|refund|expired|failed" .planning/phases/03-webhook-authoritative-orders-and-inventory/03-INVENTORY-RECONCILIATION.md` returns expected lines
- [ ] `rg -n "reservation|buffer|admin|Stripe Dashboard|operator" .planning/BACKLOG.md` returns expected lines
- [ ] `pnpm check` passes
</verification>

<success_criteria>

- All tasks completed
- Stock decrement rules are authoritative and one-way from paid webhooks
- Unpaid flows leave stock untouched
- Future admin and reservation complexity is explicitly deferred
</success_criteria>

<output>
After completion, create `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-03-SUMMARY.md`
</output>
