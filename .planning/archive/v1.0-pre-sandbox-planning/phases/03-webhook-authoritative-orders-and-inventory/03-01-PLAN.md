---
phase: 03-webhook-authoritative-orders-and-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [ORDR-01, ORDR-05]
user_setup: []
must_haves:
  truths:
    - "The operational order-state model is documented as `pending_payment`, `paid`, `closed_unpaid`, and `needs_review`."
    - "Phase 3 requirements use D1-backed language rather than stale Supabase wording for order and reconciliation state."
  artifacts:
    - path: ".planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md"
      provides: "Minimal order lifecycle and transition model"
      contains: "needs_review"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Normalized Phase 3 requirements"
      contains: "D1"
  key_links:
    - from: ".planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md"
      to: ".planning/REQUIREMENTS.md"
      via: "shared D1-backed order lifecycle language"
      pattern: "D1|pending_payment|closed_unpaid|needs_review"
---

<objective>
Define the minimal order-state model for v1 and normalize the active Phase 3 requirements so later execution planning does not inherit stale Supabase wording or a larger-than-needed state machine.

Purpose: Lock the operational state vocabulary before webhook and inventory plans build on top of it.
Output: A planning-only order-state model document plus D1-normalized Phase 3 requirement language.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-webhook-authoritative-orders-and-inventory/03-CONTEXT.md
@.planning/phases/03-webhook-authoritative-orders-and-inventory/03-RESEARCH.md
@.planning/phases/02-native-catalog-and-embedded-checkout-slice/02-02-PLAN.md
</context>

<threat_model>
<trust_boundaries>
- Browser-visible pages must not define authoritative order-state transitions.
- Operational state terminology must match D1 as the chosen store, not stale Supabase wording.
- The state model must stay small enough to remain operable at low order volume.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-03-01-A</threat_id>
  <category>Tampering</category>
  <component>Order-state source of truth</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Create a dedicated state-model doc and normalize active requirements so later plans cannot drift into stale storage assumptions.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-01-B</threat_id>
  <category>Elevation of Privilege</category>
  <component>MVP scope control</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Document no-reservation semantics and the purpose of `needs_review` so exception handling does not expand into hidden v2 scope.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write the minimal order-state model</name>
  <files>.planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md</files>
  <read_first>.planning/phases/03-webhook-authoritative-orders-and-inventory/03-CONTEXT.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-RESEARCH.md, .planning/phases/02-native-catalog-and-embedded-checkout-slice/02-02-PLAN.md</read_first>
  <action>Create `03-ORDER-STATE-MODEL.md` as a planning-only state contract. Define each v1 state (`pending_payment`, `paid`, `closed_unpaid`, `needs_review`), the allowed transitions, which Stripe-triggered situations lead to each state, and which transitions are explicitly forbidden to the browser or return pages. Include a short section on why v1 does not need a larger fulfillment-style taxonomy at the expected launch volume. Document the no-reservation oversell posture and how `needs_review` absorbs rare anomalies without redesigning the state machine.</action>
  <verify>rg -n "pending_payment|paid|closed_unpaid|needs_review|browser|no reservation|oversell" .planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md</verify>
  <acceptance_criteria>
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md` contains `pending_payment`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md` contains `paid`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md` contains `closed_unpaid`
    - `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md` contains `needs_review`
  </acceptance_criteria>
  <done>The minimal operational state vocabulary is documented and safe for downstream plans to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize active requirements to D1-backed order language</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md, .planning/phases/03-webhook-authoritative-orders-and-inventory/03-CONTEXT.md</read_first>
  <action>Update `REQUIREMENTS.md` so active Phase 3 requirement language matches the locked D1 posture. Rewrite `ORDR-01` to refer to D1-backed order lifecycle state instead of Supabase. Rewrite `OPER-01` to refer to Stripe payment records reconciled against D1-backed order and inventory state. Preserve `ORDR-05` as the explicit no-reservation rule. If any explanatory wording around Phase 3 still implies browser-side authority, tighten it to match the locked server-only model.</action>
  <verify>rg -n "ORDR-01|OPER-01|ORDR-05|D1|Supabase|browser" .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `D1` in `ORDR-01`
    - `.planning/REQUIREMENTS.md` contains `D1` in `OPER-01`
    - `.planning/REQUIREMENTS.md` does not contain `Supabase` in `ORDR-01` or `OPER-01`
    - `.planning/REQUIREMENTS.md` keeps `ORDR-05` explicit about no reservation before payment
  </acceptance_criteria>
  <done>Phase 3 requirements match the D1-backed operational-state model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "pending_payment|paid|closed_unpaid|needs_review|no reservation" .planning/phases/03-webhook-authoritative-orders-and-inventory/03-ORDER-STATE-MODEL.md` returns expected lines
- [ ] `rg -n "ORDR-01|OPER-01|D1|Supabase" .planning/REQUIREMENTS.md` returns expected lines
- [ ] `pnpm check` passes
</verification>

<success_criteria>

- All tasks completed
- The minimal order-state model is explicit and reusable
- Phase 3 requirement wording no longer depends on stale Supabase terminology
- No-reservation semantics remain clearly in scope for v1
</success_criteria>

<output>
After completion, create `.planning/phases/03-webhook-authoritative-orders-and-inventory/03-01-SUMMARY.md`
</output>
