---
phase: 01-runtime-and-guardrails
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [.planning/adrs/ADR-003-boxnow-and-cutover.md, .planning/BACKLOG.md, .planning/LAUNCH-READINESS.md]
autonomous: true
requirements: [DEPL-01]
user_setup: []
must_haves:
  truths:
    - "Cutover documentation defines full native migration without a Fourthwall fallback, but still defines a concrete rollback posture."
    - "Launch/readiness docs include human review gates and a deployment-level stop or rollback path instead of relying on an alternate storefront."
  artifacts:
    - path: ".planning/adrs/ADR-003-boxnow-and-cutover.md"
      provides: "Cutover and rollback ADR aligned to full migration"
      contains: "no Fourthwall fallback"
    - path: ".planning/LAUNCH-READINESS.md"
      provides: "Launch gates aligned to deployment-level rollback"
      contains: "rollback"
    - path: ".planning/BACKLOG.md"
      provides: "Future milestone backlog aligned to the new cutover model"
      contains: "cutover"
  key_links:
    - from: ".planning/adrs/ADR-003-boxnow-and-cutover.md"
      to: ".planning/LAUNCH-READINESS.md"
      via: "shared rollback and kill-switch language"
      pattern: "rollback|kill-switch|go-live"
---

<objective>
Define the brownfield cutover and rollback guardrails for a full migration, then align launch/readiness and backlog docs so later milestones do not assume a Fourthwall safety net.

Purpose: Keep the migration operationally safe even though the user rejected a parallel external-store fallback.
Output: Updated cutover ADR, launch-readiness checklist, and future-milestone backlog language aligned to full native cutover with deployment-level rollback.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-and-guardrails/01-CONTEXT.md
@.planning/phases/01-runtime-and-guardrails/01-RESEARCH.md
@.planning/adrs/ADR-003-boxnow-and-cutover.md
@.planning/BACKLOG.md
@.planning/LAUNCH-READINESS.md
</context>

<threat_model>
<trust_boundaries>
- Shopper-facing cutover behavior must not depend on an external fallback storefront.
- Human review gates must exist before any native go-live milestone proceeds.
- Rollback must be defined as deployment or feature disablement behavior, not as browser-side rerouting to a second commerce platform.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-01-03-A</threat_id>
  <category>Denial of Service</category>
  <component>Cutover strategy</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Document a kill-switch and redeploy rollback posture so native commerce can be disabled or reverted without needing Fourthwall.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-01-03-B</threat_id>
  <category>Repudiation</category>
  <component>Launch and review gates</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Align backlog and launch-readiness docs so later milestones require explicit human review before go-live.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ADR-003 for full migration with deployment-level rollback</name>
  <files>.planning/adrs/ADR-003-boxnow-and-cutover.md</files>
  <read_first>.planning/adrs/ADR-003-boxnow-and-cutover.md, .planning/phases/01-runtime-and-guardrails/01-CONTEXT.md, .planning/phases/01-runtime-and-guardrails/01-RESEARCH.md</read_first>
  <action>Update `ADR-003-boxnow-and-cutover.md` so its cutover strategy matches the locked Phase 1 decision: native commerce fully replaces the current Fourthwall handoff, there is no planned Fourthwall fallback, and rollback is defined as deployment-level reversal or temporarily disabling native commerce entry points rather than maintaining an alternate storefront. Keep BOX NOW scope thin for v1, but make the cutover section explicit about acceptable rollback mechanisms such as redeploying the last known-good revision or hiding the `/shop/` purchase path behind a maintenance or kill-switch response if critical issues are discovered.</action>
  <verify>rg -n "full replacement|no Fourthwall fallback|rollback|kill-switch|last known-good" .planning/adrs/ADR-003-boxnow-and-cutover.md</verify>
  <acceptance_criteria>
    - `.planning/adrs/ADR-003-boxnow-and-cutover.md` contains `no Fourthwall fallback`
    - `.planning/adrs/ADR-003-boxnow-and-cutover.md` contains `rollback`
    - `.planning/adrs/ADR-003-boxnow-and-cutover.md` contains `kill-switch` or `last known-good`
    - `.planning/adrs/ADR-003-boxnow-and-cutover.md` keeps BOX NOW v1 scope in a thin/manual-server context
  </acceptance_criteria>
  <done>ADR-003 defines a full native cutover and an acceptable rollback posture without depending on Fourthwall.</done>
</task>

<task type="auto">
  <name>Task 2: Align backlog and launch-readiness docs to the new cutover model</name>
  <files>.planning/BACKLOG.md, .planning/LAUNCH-READINESS.md</files>
  <read_first>.planning/BACKLOG.md, .planning/LAUNCH-READINESS.md, .planning/adrs/ADR-003-boxnow-and-cutover.md</read_first>
  <action>Update `BACKLOG.md` and `LAUNCH-READINESS.md` so future sandbox and go-live milestones assume a full native migration with explicit human review, deployment rollback, and kill-switch criteria instead of an external-store fallback. Preserve their role as future milestone inputs, but remove or rewrite any language that implies cutover safety depends on leaving Fourthwall available. Add checklist language that later go-live work must verify rollback procedure, operational kill-switch behavior, and human sign-off before launch.</action>
  <verify>rg -n "rollback|kill-switch|human review|Fourthwall|go-live" .planning/BACKLOG.md .planning/LAUNCH-READINESS.md</verify>
  <acceptance_criteria>
    - `.planning/LAUNCH-READINESS.md` contains `rollback`
    - `.planning/LAUNCH-READINESS.md` contains `kill-switch`
    - `.planning/BACKLOG.md` does not require a Fourthwall fallback for future cutover work
    - `.planning/BACKLOG.md` and `.planning/LAUNCH-READINESS.md` both preserve human review gates for later milestones
  </acceptance_criteria>
  <done>Future cutover and launch docs align to the full-migration posture and explicit review gates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "no Fourthwall fallback|rollback|kill-switch" .planning/adrs/ADR-003-boxnow-and-cutover.md` returns expected lines
- [ ] `rg -n "rollback|kill-switch|human review|Fourthwall" .planning/BACKLOG.md .planning/LAUNCH-READINESS.md` returns expected lines
- [ ] `pnpm check` passes
</verification>

<success_criteria>

- All tasks completed
- Cutover docs no longer rely on an external commerce fallback
- Rollback is defined as deployment or feature disablement behavior
- Launch and backlog docs maintain explicit human review gates for later milestones
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-and-guardrails/01-03-SUMMARY.md`
</output>
