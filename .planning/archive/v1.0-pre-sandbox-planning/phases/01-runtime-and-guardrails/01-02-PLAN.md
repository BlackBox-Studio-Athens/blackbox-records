---
phase: 01-runtime-and-guardrails
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [.planning/adrs/ADR-002-commerce-boundaries.md, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [DEPL-02, CATA-03, SECU-01]
user_setup: []
must_haves:
  truths:
    - "The planning docs define Stripe as catalog and payment-event authority, D1 as inventory and order-lifecycle state, and the browser as a non-authoritative actor."
    - "Project requirements and ADRs explicitly say that authoritative inventory/order writes never come from the browser."
    - "Stripe terminology and API-version policy are documented with an explicit note about current official docs using `ui_mode: embedded`."
  artifacts:
    - path: ".planning/adrs/ADR-002-commerce-boundaries.md"
      provides: "Commerce trust-boundary ADR aligned to D1"
      contains: "D1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements normalized to D1 and current Stripe terminology"
      contains: "ui_mode"
  key_links:
    - from: ".planning/adrs/ADR-002-commerce-boundaries.md"
      to: ".planning/REQUIREMENTS.md"
      via: "shared D1 ownership and browser-write prohibition"
      pattern: "D1|browser.*must not write|ui_mode"
---

<objective>
Finalize the commerce-boundary ADR and the related requirements so the repo has one coherent trust model: Stripe owns catalog and payment truth, D1 owns operational state, and server-only code owns all privileged writes and secrets.

Purpose: Prevent downstream checkout, webhook, and shipping plans from inheriting stale Supabase assumptions or outdated Stripe terminology.
Output: Updated ADR-002 and normalized requirements covering D1 ownership, browser restrictions, and Stripe API/version terminology.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-and-guardrails/01-CONTEXT.md
@.planning/phases/01-runtime-and-guardrails/01-RESEARCH.md
@.planning/phases/01-runtime-and-guardrails/01-01-SUMMARY.md
@.planning/adrs/ADR-002-commerce-boundaries.md
@.planning/REQUIREMENTS.md
</context>

<threat_model>
<trust_boundaries>
- Browser reads safe storefront projections only.
- Worker routes own Stripe API calls, webhook signature verification, and D1 writes.
- D1 stores operational state only; Stripe remains sellable catalog and payment-event authority.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-01-02-A</threat_id>
  <category>Tampering</category>
  <component>Order and inventory state model</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Update ADR-002 and requirements so browser writes to authoritative order/inventory state are explicitly forbidden and webhook payment success is authoritative.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-01-02-B</threat_id>
  <category>Repudiation</category>
  <component>Stripe API policy</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Record an explicit Stripe API-version and terminology policy so later implementation does not rely on ambiguous or stale documentation language.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ADR-002 around Stripe plus D1 ownership boundaries</name>
  <files>.planning/adrs/ADR-002-commerce-boundaries.md</files>
  <read_first>.planning/adrs/ADR-002-commerce-boundaries.md, .planning/phases/01-runtime-and-guardrails/01-CONTEXT.md, .planning/phases/01-runtime-and-guardrails/01-RESEARCH.md</read_first>
  <action>Replace the current Supabase-centric language in `ADR-002-commerce-boundaries.md` with the locked Phase 1 model: Stripe is the source of truth for products, prices, checkout sessions, and payment event truth; D1 is the source of truth for inventory counts and order lifecycle state; browser code never writes authoritative inventory or paid-order state; webhook-confirmed payment success is authoritative for paid status and stock decrement; v1 still has no reservation logic. Add an explicit policy section that later implementation must pin the Stripe API version deliberately and must follow the current official docs rather than older internal wording when enum names or flow terminology change.</action>
  <verify>rg -n "Stripe.*source of truth|D1|browser.*must not write|webhook-confirmed payment success|API version" .planning/adrs/ADR-002-commerce-boundaries.md</verify>
  <acceptance_criteria>
    - `.planning/adrs/ADR-002-commerce-boundaries.md` contains `D1`
    - `.planning/adrs/ADR-002-commerce-boundaries.md` contains `browser` and `must not write` in the same boundary rule
    - `.planning/adrs/ADR-002-commerce-boundaries.md` contains `webhook-confirmed payment success`
    - `.planning/adrs/ADR-002-commerce-boundaries.md` contains `API version`
  </acceptance_criteria>
  <done>ADR-002 matches the locked D1 trust model and documents Stripe version-policy handling.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize requirements for D1 ownership and current Stripe embedded terminology</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-runtime-and-guardrails/01-CONTEXT.md, .planning/phases/01-runtime-and-guardrails/01-RESEARCH.md, .planning/adrs/ADR-002-commerce-boundaries.md</read_first>
  <action>Update `REQUIREMENTS.md` so all operational-state references use D1 rather than Supabase. Specifically, rewrite `ORDR-01` and `OPER-01` to name D1 as the operational state store. Keep `CATA-03` explicit that Astro content collections and Decap CMS remain editorial, not sellable catalog authority. Update `CHKO-01` to preserve the embedded Checkout intent but add a dated note that, as of 2026-04-19, Stripe's current official Checkout Sessions API reference uses `ui_mode: embedded`; if previous project text referenced `embedded_page`, treat that as stale terminology to be normalized during implementation rather than as the enum to send. Ensure `SECU-01` and `SECU-02` still reinforce server-only secrets and no browser-side authoritative writes.</action>
  <verify>rg -n "ORDR-01|OPER-01|CATA-03|CHKO-01|SECU-01|SECU-02|D1|ui_mode|embedded_page|embedded" .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `D1` in `ORDR-01`
    - `.planning/REQUIREMENTS.md` contains `D1` in `OPER-01`
    - `.planning/REQUIREMENTS.md` preserves `Astro collections` or equivalent editorial-ownership wording in `CATA-03`
    - `.planning/REQUIREMENTS.md` contains `as of 2026-04-19` and `ui_mode: embedded` in `CHKO-01`
    - `.planning/REQUIREMENTS.md` treats `embedded_page` as older or stale wording, not as the current enum to send
  </acceptance_criteria>
  <done>Requirements consistently use D1 for operational state and document the current Stripe embedded Checkout terminology safely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "D1|browser.*must not write|webhook-confirmed payment success|API version" .planning/adrs/ADR-002-commerce-boundaries.md` returns expected lines
- [ ] `rg -n "ORDR-01|OPER-01|CHKO-01|ui_mode|embedded_page|embedded" .planning/REQUIREMENTS.md` returns expected lines
- [ ] `pnpm check` passes
</verification>

<success_criteria>

- All tasks completed
- ADR-002 and REQUIREMENTS agree on Stripe-vs-D1 ownership
- Requirements no longer mention Supabase as the v1 operational state store
- Stripe terminology drift is documented with an explicit dated note and version-policy guidance
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-and-guardrails/01-02-SUMMARY.md`
</output>
