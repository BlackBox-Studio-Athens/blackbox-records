---
phase: 09-greece-only-box-now-shipping
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
requirements: [SHIP-01, SECU-02]
---

# 09-03 Plan - Add Backend Checkout Preflight For Greece-Only Locker Selection

## Objective

Require valid Greece-only locker selection before the Worker starts checkout.

## Scope Boundary

This commit validates checkout input. It does not persist paid-order shipping state or automate fulfillment.

## Tasks

1. Extend `StartCheckout` input with the approved locker snapshot.
2. Validate Greece-only constraints in the Worker before creating Checkout Sessions.
3. Keep browser-submitted locker data non-authoritative until webhook/order persistence.
4. Update OpenAPI/client artifacts and tests.

## Verification

- checkout cannot start without valid Greece-only locker data
- invalid country or missing locker data receives a clear non-500 response
- Stripe session creation remains Worker-owned
- `pnpm generate:api`, `pnpm test:unit`, `pnpm check`, and `pnpm build` pass

## Explicit Exclusions

- No paid-order shipping persistence
- No BOX NOW partner API calls
- No fulfillment status
- No stock behavior changes
