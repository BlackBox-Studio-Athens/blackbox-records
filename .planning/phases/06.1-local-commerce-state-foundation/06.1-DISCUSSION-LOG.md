# Phase 6.1 Discussion Log

**Date:** 2026-04-20
**Status:** Closed

## Why this phase was inserted

The shop UI and projection should be stable before checkout work begins, but the project still needs a real local D1 + Prisma foundation before Stripe integration starts.

Locked outcome:
- Phase 6.1 is the hard gate between native storefront work and Stripe checkout work.
- It introduces local D1 + Prisma and the migration path.
- It preserves the Phase 6 UI-facing shop contract.
- It does not absorb webhook or order-lifecycle work from Phase 8.
