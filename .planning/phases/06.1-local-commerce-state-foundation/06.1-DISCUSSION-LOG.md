# Phase 6.1 Discussion Log

## Locked Backend Defaults

- D1 and Prisma live inside the separate Worker backend.
- D1 owns operational state and mappings, not editorial content.
- Stripe pricing authority remains deferred to later checkout work.
- The storefront contract from Phase 6 must not change.

## Why Phase 6.1 Changed

The earlier version framed this phase around a generic shop repository boundary. The corrected architecture makes it explicit that D1 and Prisma arrive inside the Worker backend, not inside the static frontend.

---
*Logged: 2026-04-20*
