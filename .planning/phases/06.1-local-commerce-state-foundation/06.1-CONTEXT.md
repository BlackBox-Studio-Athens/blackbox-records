# Phase 6.1: Worker Commerce State Foundation - Context

**Gathered:** 2026-04-20
**Status:** Planning complete

<domain>
## Phase Boundary

Phase 6.1 introduces D1 and Prisma inside the separate Worker backend after the static storefront contract already exists. It does not introduce Stripe checkout, webhook semantics, or the full order lifecycle implementation from Phase 8.

</domain>

<decisions>
## Implementation Decisions

### Backend database scope
- **D-01:** Phase 6.1 bootstraps local D1 and binding names for the Worker backend.
- **D-02:** Prisma is used for runtime access on D1, and migrations remain SQL applied through Wrangler.
- **D-03:** The first D1-backed reads are limited to backend commerce-state concerns needed behind Worker repositories and APIs.
- **D-04:** Full order lifecycle and authoritative paid-state semantics remain deferred to Phase 8.

### Repository and API boundary
- **D-05:** The Phase 6 storefront contract must not change in this phase.
- **D-06:** Backend repositories may begin moving temporary variant or mapping reads from static/fixture data to D1-backed data where useful.
- **D-07:** Price authority is still not D1's responsibility; Stripe remains the later authority for sellable commerce data.
- **D-07.1:** Repositories, use cases, and HTTP endpoints remain TypeScript-only and Hono stays limited to the HTTP interface layer.
- **D-07.2:** Repository and use-case names must use the approved domain language rather than generic service/helper terminology.
- **D-07.3:** D1 and Prisma changes require backend-local tests in addition to route smoke coverage.

### Phase shape
- **D-08:** This phase is about the separate backend, not the frontend.
- **D-09:** D1 and Prisma are introduced behind Worker repositories and backend API seams.
- **D-10:** Phase 7 depends on this phase so checkout work starts only after backend state foundations exist.

</decisions>

<specifics>
## Specific Ideas

- Keep D1 focused on operational state and mappings, not editorial duplication.
- Keep the storefront contract stable and treat D1 as one backend source behind that contract.
- Make Phase 6.1 the one place where D1 + Prisma assumptions become concrete before Stripe arrives.

</specifics>

<canonical_refs>
## Canonical References

- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
- `.planning/phases/05.1-commerce-domain-architecture-and-source-of-truth-research/05.1-RESEARCH.md`
- `.planning/phases/06-native-storefront-slice/06-CONTEXT.md`

</canonical_refs>

<code_context>
## Existing Code Insights

- Phase 6 introduces the stable `CatalogItem` and temporary `VariantSnapshot` storefront contract.
- The backend now has a real `COMMERCE_DB` D1 binding, Worker-compatible Prisma runtime access, and initial repository seams.
- The migration workflow is now explicit through committed SQL in `apps/backend/prisma/migrations/` and Wrangler-owned apply commands.
- Local seed SQL now populates the first D1-backed mapping and inventory rows for current storefront cases.
- The first D1-backed read path now exists through a backend application reader that resolves offer availability by `catalogItemSlug`.
- The Worker runtime/environment contract from Phase 5 must already be in place before this phase executes.

</code_context>

<deferred>
## Deferred Ideas

- live Stripe pricing authority
- checkout session creation
- webhook verification
- authoritative order lifecycle schema and post-payment inventory mutation

</deferred>

---

*Phase: 06.1-local-commerce-state-foundation*
*Context gathered: 2026-04-20*
