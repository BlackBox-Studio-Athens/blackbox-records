---
phase: 06-native-storefront-slice
plan: 04
type: execute
wave: 2
depends_on: ['06-01', '06-02']
requirements: [CATA-01]
---

# 06-04 Plan - Replace The Legacy `/shop/` Redirect With The Canonical `/store/` Collection Route

## Objective

Replace the redirect-only shop entry with a native static collection route.

## Tasks

1. Remove the redirect-only behavior.
2. Render the collection route from the `StoreItem` projection.
3. Keep the route compatible with the existing app shell.

## Verification

- `/store/` is native and `/shop/` redirects to it
- collection route uses the new projection
- app-shell behavior remains intact
