---
plan_id: 12-12
phase: 12
status: completed
created: 2026-05-15
completed: 2026-05-15
---

# 12-12 Plan - Player Provider Warmup Boundary Hardening

## Outcome

Move player provider origin warmup details out of `AppShellRoot` and into the closed `player` module with direct
characterization coverage.

## Scope

- Characterize provider origin warmup behavior:
  - Bandcamp and Tidal origins get preconnect and DNS prefetch links
  - already-warmed origins are skipped
  - existing document links are not duplicated
  - missing document head does not throw
- Update `AppShellRoot` to delegate provider origin warmup while preserving player modal/session behavior.
- Update player module ownership docs and boundary manifest for the new player entrypoint.
- Keep public routing, overlays, StoreCart behavior, checkout contracts, Worker behavior, D1, Stripe, BOX NOW,
  generated API output, `.planning/config.json`, and worktree settings unchanged.

## Verification

- `pnpm --filter @blackbox/web test:unit -- src/components/app-shell/player-provider-warmup.test.ts`
- `pnpm audit:module-boundaries`
- `pnpm depcruise:boundaries`
- `pnpm test:unit`
- `pnpm check`
- `pnpm build`

## Browser Use

If Browser Use is available, validate player open/minimize/reopen/stop and console cleanliness. If Browser Use remains
unavailable, record the blocker in the summary rather than substituting an external browser.
