---
plan_id: 12-55
phase: 12
status: completed
created: 2026-05-16
completed: 2026-05-16
---

# 12-55 Plan - cms-admin Temporary-Open Closure

## Outcome

Close the `cms-admin` temporary-open exception now that the Decap config hotspot has explicit route, runtime, YAML, and
field/collection generation seams with direct tests.

## Scope

- Move `cms-admin` from `open-temporary` to `closed` in the module boundary manifest and planning module table.
- Declare the admin media route as an explicit `cms-admin` entrypoint alongside `/admin/` and `/admin/config.yml`.
- Update the manifest validator approved temporary-open set so only `app-shell` remains approved.
- Update architecture tests to prove `app-shell` still requires temporary-open metadata and `cms-admin` cannot be
  reopened as a temporary exception without a deliberate validator change.
- Update the `cms-admin` module canvas from exit criteria to closure evidence.
- Do not change admin runtime behavior, shopper-facing behavior, backend contracts, commerce authority, or deployment
  configuration.

## Verification

- `pnpm --filter @blackbox/backend exec vitest run test/architecture/module-boundaries-manifest.test.ts`
- `pnpm audit:module-boundaries`
- `pnpm depcruise:boundaries`
- `pnpm test:unit`
- `pnpm check`
- `pnpm build`
