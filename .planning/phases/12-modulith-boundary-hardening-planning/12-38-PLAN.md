---
plan_id: 12-38
phase: 12
status: completed
created: 2026-05-16
completed: 2026-05-16
---

# 12-38 Plan - Overlay Open Coordination

## Outcome

Move overlay open coordination out of `AppShellRoot` into the `overlay` app-shell internals with direct characterization
tests.

## Scope

- Extract overlay route parsing, abort handling, cached/loading state seeding, optional history writes, fragment fetch,
  stale-route protection, focus scheduling, and document-navigation fallback into `shell-overlay-navigation`.
- Preserve overlay open/close behavior, shell routing, player behavior, StoreCart behavior, checkout contracts, Worker
  behavior, D1, Stripe, BOX NOW, generated API output, `.planning/config.json`, and worktree behavior.
- Keep React refs/state ownership in `AppShellRoot`.
- Keep `eslint-plugin-boundaries` active while suppressing the resolver's known multi-project warning through its
  resolver option, not by weakening boundary rules.
- Record the direct app-shell-scoped Vitest command for faster Phase 12 refactor loops before the full required gates.
- Per user decision, defer both:
  - formal internal-submodule planning/tooling changes;
  - Nx adoption.

## Verification

- AppShellRoot line-count measurement
- `pnpm --filter @blackbox/web exec vitest run shell-overlay-navigation`
- `pnpm --filter @blackbox/web exec vitest run src/components/app-shell`
- `pnpm test:unit`
- `pnpm audit:module-boundaries`
- `pnpm depcruise:boundaries`
- `pnpm check`
- `pnpm build`
