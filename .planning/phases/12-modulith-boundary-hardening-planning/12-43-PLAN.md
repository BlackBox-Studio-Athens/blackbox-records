---
plan_id: 12-43
phase: 12
status: completed
created: 2026-05-16
completed: 2026-05-16
---

# 12-43 Plan - Decap Runtime Config Extraction

## Outcome

Move Decap admin runtime/base-path decisions out of the generated YAML builder so admin route wiring no longer depends on
mixed config-building internals.

## Scope

- Extract local-backend selection and site-root URL resolution into `apps/web/src/lib/admin/decap-runtime-config.ts`.
- Move the existing characterization tests for those decisions next to the extracted helper.
- Update `/admin/config.yml` to import route/runtime helpers from the new seam while keeping YAML generation through
  `buildDecapConfig`.
- Preserve generated admin config output, shopper-facing catalog, checkout, stock, order, Stripe, D1, BOX NOW, Worker
  behavior, generated API output, `.planning/config.json`, and worktree behavior.

## Verification

- `pnpm --filter @blackbox/web exec vitest run src/lib/admin`
- `pnpm check`
- `pnpm test:unit`
- `pnpm build`
