---
plan_id: 12-56
phase: 12
status: completed
created: 2026-05-16
completed: 2026-05-16
---

# 12-56 Plan - app-shell Temporary-Open Closure

## Outcome

Close the remaining `app-shell` temporary-open exception after the AppShellRoot extraction run brought the composition root
inside the Phase 12 target band and moved shell behavior into named, tested internal helpers.

## Scope

- Move `app-shell` from `open-temporary` to `closed` in the module boundary manifest and planning module table.
- Update the manifest validator approved temporary-open set to empty.
- Update architecture tests to prove reopening `app-shell`, `cms-admin`, or another module as temporary-open fails without
  an explicit validator change, while metadata is still required for any attempted temporary-open module.
- Update the `app-shell` module canvas from exit criteria to closure evidence.
- Preserve `AppShell.astro` as the stable public shell mount surface.
- Do not change shopper-visible shell behavior, app routing, overlay behavior, player behavior, StoreCart behavior,
  checkout contracts, backend behavior, or deployment/runtime configuration.

## Verification

- `pnpm --filter @blackbox/backend exec vitest run test/architecture/module-boundaries-manifest.test.ts`
- `pnpm --filter @blackbox/web exec vitest run src/components/app-shell`
- `pnpm audit:module-boundaries`
- `pnpm depcruise:boundaries`
- `pnpm test:unit`
- `pnpm check`
- `pnpm build`
