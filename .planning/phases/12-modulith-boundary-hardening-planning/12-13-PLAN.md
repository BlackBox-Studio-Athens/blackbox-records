---
plan_id: 12-13
phase: 12
status: completed
created: 2026-05-15
completed: 2026-05-15
---

# 12-13 Plan - Player Trigger Data Boundary Hardening

## Outcome

Move player trigger dataset parsing and default-provider selection out of `AppShellRoot` and into the closed `player`
module with direct characterization coverage.

## Scope

- Characterize player trigger data behavior:
  - provider data is read from the rendered listen-trigger dataset
  - player title is read from the rendered listen-trigger dataset
  - Bandcamp remains the default provider when both providers exist
  - the available provider is selected when the preferred provider is missing
- Update `AppShellRoot` to consume the player module helpers instead of owning dataset parsing or provider priority.
- Keep public routing, overlays, StoreCart behavior, checkout contracts, Worker behavior, D1, Stripe, BOX NOW,
  generated API output, `.planning/config.json`, and worktree settings unchanged.

## Verification

- `pnpm --filter @blackbox/web test:unit -- src/components/app-shell/player-provider-data.test.ts`
- `pnpm audit:module-boundaries`
- `pnpm depcruise:boundaries`
- `pnpm test:unit`
- `pnpm check`
- `pnpm build`

## Browser Use

If Browser Use is available, validate player open/minimize/reopen/stop and console cleanliness. If Browser Use remains
unavailable, record the blocker in the summary rather than substituting an external browser.
