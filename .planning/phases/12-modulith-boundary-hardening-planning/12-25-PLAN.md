---
plan_id: 12-25
phase: 12
status: completed
created: 2026-05-15
completed: 2026-05-15
---

# 12-25 Plan - Shell Prefetch Intent Boundary Hardening

## Outcome

Move shell document hover/focus prefetch intent classification out of `AppShellRoot` into an internal app-shell helper with
direct characterization coverage.

## Scope

- Characterize prefetch intent behavior:
  - non-element event targets do nothing
  - nearest player cards warm provider origins
  - shell-section and overlay anchors prefetch through the supplied classifiers
- Update `AppShellRoot` to delegate prefetch intent classification while preserving shell routing, overlay behavior,
  player behavior, StoreCart behavior, checkout, Worker, D1, Stripe, BOX NOW, generated API output,
  `.planning/config.json`, and worktree behavior.

## Verification

- `pnpm --filter @blackbox/web test:unit -- src/components/app-shell/shell-prefetch-intent.test.ts`
- `pnpm audit:module-boundaries`
- `pnpm depcruise:boundaries`
- `pnpm test:unit`
- `pnpm check`
- `pnpm build`

## Browser Use

If Browser Use is available, validate header/footer/mobile section switching, detail overlay open/close, player
open/minimize/reopen/stop, cart drawer open/close, and console cleanliness. If Browser Use remains unavailable, record
the blocker in the summary rather than substituting an external browser.
