---
plan_id: 12-15
phase: 12
status: completed
created: 2026-05-15
completed: 2026-05-15
---

# 12-15 Plan - Shell Body State Class Boundary Hardening

## Outcome

Move shell-owned body class synchronization out of `AppShellRoot` into an internal app-shell helper with direct
characterization coverage.

## Scope

- Characterize body class behavior:
  - player modal and overlay classes are toggled from shell state
  - cleanup removes both shell-owned body classes
- Update `AppShellRoot` to delegate body class synchronization while preserving player modal and overlay behavior.
- Keep routing, target scrolling, StoreCart behavior, checkout contracts, Worker behavior, D1, Stripe, BOX NOW,
  generated API output, `.planning/config.json`, and worktree settings unchanged.

## Verification

- `pnpm --filter @blackbox/web test:unit -- src/components/app-shell/shell-body-state.test.ts`
- `pnpm audit:module-boundaries`
- `pnpm depcruise:boundaries`
- `pnpm test:unit`
- `pnpm check`
- `pnpm build`

## Browser Use

If Browser Use is available, validate overlay open/close, player modal open/close, and console cleanliness. If Browser
Use remains unavailable, record the blocker in the summary rather than substituting an external browser.
