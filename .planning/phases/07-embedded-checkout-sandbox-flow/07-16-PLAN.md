---
phase: 07-embedded-checkout-sandbox-flow
plan: 16
type: validate
wave: 6
depends_on: ['07-11', '07-15']
requirements: [CHKO-03, CHKO-04, CHKO-05]
status: deferred
---

# 07-16 Plan - Validate The Local And Sandbox Checkout Loop With Real Stripe Sandbox Mappings

## Objective

Prove the Phase 7 checkout loop locally and in sandbox with selected real Stripe sandbox price mappings, after Stripe account access exists.

## Scope Boundary

This commit captures validation evidence and any required docs/scripts. Do not implement webhooks, order authority, stock decrement, shipping, or production cutover.

## Deferred Status

This plan is blocked until Stripe account access exists. Phase 7 mock checkout, API contract, cart/checkout UI, return UI, local all-items mock readiness, and Browser Use mock UAT are complete enough to proceed with non-secret Phase 8 backend order groundwork.

Required later access:

- real Stripe test publishable key (`pk_test_*`)
- real Stripe test secret key (`sk_test_*`)
- real Stripe test Price ID (`price_*`) for selected mapped variants
- Stripe webhook signing secret (`STRIPE_WEBHOOK_SECRET`)
- Stripe products/prices configured in the account
- sandbox Worker URL and webhook endpoint configuration
- Browser Use evidence against real Stripe test Checkout

## Tasks

1. After Stripe access exists, run `pnpm checkout:preflight:stripe-test`.
2. Use `pnpm dev:stack:stripe-test` with ignored local Stripe test Price mappings to validate local frontend-to-Worker-to-Stripe embedded Checkout for selected smoke items.
3. Validate the canonical item-option URL, cart icon, cart drawer, checkout summary, embedded Checkout mount, and return/retry UI through Browser Use.
4. Validate sandbox Worker deployment and frontend backend URL configuration.
5. Confirm real sandbox `VariantStripeMapping` values are documented or applied without committing Stripe test Price IDs.
6. Capture known Phase 8 gaps around webhook authority and stock mutation.

## Verification

- `pnpm checkout:preflight:stripe-test` passes before starting the real Stripe test stack.
- local checkout loop reaches Stripe sandbox for selected smoke items.
- sandbox checkout loop reaches Stripe sandbox for selected smoke items.
- canonical item-option URL and legacy redirects behave correctly.
- cart/checkout UX passes browser smoke checks on desktop and mobile.
- required Stripe secret, mock mapping, and selected real mapping setup is documented.
- `pnpm test:unit`, `pnpm check`, and `pnpm build` pass.

## Explicit Exclusions

- No live-mode Stripe keys.
- No production deployment switch.
- No webhook paid-order authority.
- No BOX NOW shipping.
