---
phase: 07-embedded-checkout-sandbox-flow
plan: 07
type: validate
wave: 4
depends_on: ["07-06"]
requirements: [CHKO-03]
---

# 07-07 Plan - Validate The Local And Sandbox Checkout Loop With Real Stripe Sandbox Mappings

## Objective

Prove the Phase 7 checkout loop locally and in sandbox with real Stripe sandbox price mappings.

## Scope Boundary

This commit captures validation evidence and any required docs/scripts. Do not implement webhooks, order authority, stock decrement, shipping, or production cutover.

## Tasks

1. Use `pnpm dev:stack:stripe-test` with ignored local Stripe test Price mappings to validate local frontend-to-Worker-to-Stripe embedded Checkout.
2. Use `pnpm dev:stack:stripe-mock` only as a request-shape and local no-network sanity path; do not count it as end-to-end payment evidence.
3. Validate sandbox Worker deployment and frontend backend URL configuration.
4. Confirm real sandbox `VariantStripeMapping` values are documented or applied without committing Stripe test Price IDs.
5. Capture known Phase 8 gaps around webhook authority and stock mutation.

## Verification

- local checkout loop reaches Stripe sandbox
- stripe-mock sanity path can start checkout and render the frontend mock panel
- sandbox checkout loop reaches Stripe sandbox
- required Stripe secret and mapping setup is documented
- `pnpm test:unit`, `pnpm check`, and `pnpm build` pass

## Explicit Exclusions

- No live-mode Stripe keys
- No production deployment switch
- No webhook paid-order authority
- No BOX NOW shipping
