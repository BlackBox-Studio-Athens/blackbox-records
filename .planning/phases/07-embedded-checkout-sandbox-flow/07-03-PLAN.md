---
phase: 07-embedded-checkout-sandbox-flow
plan: 03
type: execute
wave: 2
depends_on: ['07-01', '07-02']
requirements: [CHKO-03]
---

# 07-03 Plan - Wire The Checkout Shell To Worker Offer And Variant Reads

## Objective

Replace the checkout shell's temporary availability reads with Worker-backed offer and variant reads.

## Scope Boundary

This commit reads Worker checkout state for display only. Do not start Checkout Sessions or mount embedded Checkout yet.

## Tasks

1. Use the frontend public checkout API helper from `07-02` in `/store/[slug]/checkout/`.
2. Show Worker-known availability and checkout eligibility beside the existing editorial item recap.
3. Keep static editorial content from `StoreItem` as the UI source for title, image, and summary.
4. Add unavailable, missing backend, and API error states that do not imply a fake payment path.

## Verification

- [x] checkout shell fetches offer and variant reads from the Worker seam
- [x] unavailable items remain viewable but cannot start checkout
- [x] static fallback copy is removed where it claims payment is deferred
- [x] `pnpm test:unit`, `pnpm check`, and `pnpm build` pass

## Explicit Exclusions

- No `StartCheckout` call
- No embedded Checkout mount
- No return or retry UI
- No backend changes
