# Phase 5.1 Research - Commerce Domain Architecture And Source-Of-Truth Research

## Standard Stack

- Astro content collections for editorial content and static page generation
- separate Cloudflare Worker backend as BFF/API surface
- Stripe Products/Prices and Checkout for sellable commerce data and payment flow
- D1 plus Prisma runtime access for operational state and mappings

## Architecture Patterns

### Why the projection layer is required

- `releases` already model editorial release content and artist relationships.
- `distro` entries already model editorial card content and currently still carry `fourthwall_url`.
- Those two content sources are useful storefront inputs, but neither of them is the right place to hold live sellable state, stock, or Stripe identity.
- The native store therefore needs a projection layer that derives a stable storefront-facing `StoreItem` from editorial content without turning Astro content into the commerce authority.

### Required implementation posture

- The browser and static storefront routes should consume `StoreItem` and later `ItemAvailability`, not Stripe-native or D1-native records.
- The Worker backend is the only layer allowed to translate between storefront contracts, Stripe sellable data, and D1 mappings.
- Editorial content stays useful and minimal when it remains descriptive instead of trying to encode live stock or payment state.
- `StoreItem` should remain stable even if backend variant mappings or Stripe configuration change underneath it.

## Don't Hand-Roll

- Do not duplicate editorial content into D1.
- Do not use Stripe as the home for editorial media or prose.
- Do not expose direct browser writes to D1 or Stripe secret APIs.
- Do not treat the current `fourthwall_url` field as the future native shop identity.

## Common Pitfalls

- collapsing `StoreItem` and `Variant` into one object too early
- storing pricing, stock, Stripe IDs, or checkout-owned fields in Astro collections
- letting the frontend consume Stripe-specific IDs or D1 record identifiers directly
- coupling browse/product-detail pages directly to backend persistence shapes instead of a storefront contract
- treating the projection layer as if it were a new permanent source of truth instead of a derived view

## Downstream implementation guidance

- static frontend consumes `StoreItem` for browse/PDP
- frontend fetches `ItemAvailability` from temporary adapter or Worker API without changing UI contract
- backend repositories hide D1 and Stripe implementation details behind interfaces

## Internal Backend Interface Draft

- `EditorialContentSource`
- `StoreItemProjector`
- `StoreItemReader`
- `ItemAvailabilityReader`
- `StoreItemOptionRepository`
- `VariantStripeMappingRepository`
- `ItemAvailabilityRepository`
- `CreateCheckoutSession`
- `ReceiveStripeWebhook`

These names intentionally follow the commerce domain vocabulary rather than generic `service`, `handler`, or `manager` labels.

## External Worker API Draft

Public shopper API:

- `GET /api/store/items`
- `GET /api/store/items/:storeItemSlug`
- `GET /api/store/items/:storeItemSlug/variants`
- `POST /api/checkout/sessions`

Stripe integration API:

- `POST /api/stripe/webhooks`

Contract split:

- shopper-facing routes belong in the public OpenAPI document and generated frontend client package
- future staff-only stock routes belong in the internal OpenAPI document once Phase `06.1.1` introduces the protected backend surface
- the Stripe webhook endpoint is backend-owned integration surface, not a browser-facing client route

## Canonical ID And Linking Rules

- `ArtistId`, `ReleaseId`, and `DistroEntryId` come from Astro content entry identity.
- `StoreItemSlug` is the canonical shop route identity consumed by the storefront and public Worker API.
- `VariantId` is the canonical sellable identity behind stock, checkout, and Stripe mappings.
- backend mapping records translate:
  - content-source identity -> `StoreItemSlug`
  - `StoreItemSlug` -> one or more `VariantId`
  - `VariantId` -> Stripe product/price identity
- release pages and distro entry points must route to canonical `/store/[storeItemSlug]/` pages once native store linking replaces the current Fourthwall handoff.

## Recommendation

Implement the storefront around a stable `StoreItem` plus `ItemAvailability` contract. Keep the browser coupled only to those contracts and route slugs. Put Stripe IDs, stock values, and order state behind the Worker backend and D1 mappings.

---

_Research completed: 2026-04-20_
