# Phase 5.1: Commerce Domain Architecture And Source-Of-Truth Research - Context

**Gathered:** 2026-04-20
**Status:** Planning complete

<domain>
## Phase Boundary

Phase 5.1 is the architecture gate for the commerce domain. It freezes the entity model, source-of-truth split, IDs, mappings, and API contracts before storefront, D1, or Stripe implementation continues.

</domain>

<decisions>
## Implementation Decisions

### Domain defaults
- **D-01:** `Artist` is editorial only and not directly sellable.
- **D-02:** `Release` is editorial content that may map to one or more sellable offers.
- **D-03:** `DistroEntry` is editorial/catalog content that may map to one or more sellable offers.
- **D-04:** `ShopItem` is the canonical storefront-facing identity derived from one content source: `release` or `distro`.
- **D-05:** `Offer/SKU` is the primary sellable unit for inventory, checkout, Stripe mapping, and later order lines.

### Source of truth
- **D-06:** Astro content collections own editorial content only.
- **D-07:** Stripe owns sellable commerce data needed for checkout.
- **D-08:** D1 owns operational state plus internal mappings.

### Architecture boundaries
- **D-09:** The frontend consumes a stable `ShopItem` and `OfferSnapshot` contract rather than talking directly to Stripe or D1.
- **D-10:** The Worker backend is the only component that talks to Stripe or D1.
- **D-11:** IDs, slugs, and mappings must be explicit before implementation.

</decisions>

<specifics>
## Specific Ideas

- Use a projection layer to derive shop-facing content from `releases` and `distro`.
- Keep editorial content out of D1 and keep operational state out of Astro collections.
- Draft APIs before writing code so later phases can stay implementation-focused.

</specifics>

<canonical_refs>
## Canonical References

- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
- `.planning/PROJECT.md`
- `.planning/STATE.md`
- `src/content.config.ts`
- `src/lib/catalog-data.ts`

</canonical_refs>

<code_context>
## Existing Code Insights

- `releases` already reference `artists`.
- `distro` is still editorial content plus `fourthwall_url`.
- current helpers already aggregate content collections for browse/detail pages.

</code_context>

<deferred>
## Deferred Ideas

- exact checkout request payload implementation
- webhook idempotency implementation details
- final BOX NOW backend APIs

</deferred>

---

*Phase: 05.1-commerce-domain-architecture-and-source-of-truth-research*
*Context gathered: 2026-04-20*
