---
phase: 08-webhook-orders-and-inventory
plan: 07
type: execute
wave: 4
depends_on: ['08-06']
requirements: [ORDR-01, ORDR-04]
---

# 08-07 Plan - Add Backend Order-State Readback And Operator Reconciliation Notes

## Objective

Expose the minimum backend readback and documentation needed to inspect order state during sandbox operations.

## Scope Boundary

This is not a full operator OMS. It only supports low-volume sandbox inspection and reconciliation notes.

## Tasks

1. Add backend-only readback helpers or internal route support for order state if needed by sandbox verification.
2. Document how operators inspect needs-review orders without editing D1 directly.
3. Add tests for readback shape and protected/internal boundary if a route is introduced.
4. Align Phase 8 docs before moving to shipping work.

## Verification

- paid/unpaid/needs-review order states can be inspected safely
- no public shopper route exposes internal order details
- docs explain low-volume reconciliation behavior
- `pnpm test:unit`, `pnpm check`, and `pnpm build` pass

## Explicit Exclusions

- No full order dashboard
- No refund tooling
- No shipping automation
- No production go-live checklist
