---
phase: 08-webhook-orders-and-inventory
plan: 04
type: execute
wave: 2
depends_on: ['08-02', '08-03']
requirements: [ORDR-02, ORDR-04]
status: complete
---

# 08-04 Plan - Add Shared Stripe Checkout Reconciliation Use Case

## Objective

Create one backend reconciliation use case that normalizes Stripe Checkout Session state for both webhook handling and `ReadCheckoutState`.

## Scope Boundary

This commit adds reconciliation logic using backend types, mocked gateway/session inputs, and fixture data. It does not require live Stripe account access and does not yet perform stock decrement.

## Tasks

1. [x] Add a business-named reconciliation use case for Stripe Checkout Session state.
2. [x] Map paid, unpaid, expired, open, and needs-review outcomes into app-owned order states.
3. [x] Reuse the normalization path from `ReadCheckoutState` without making browser reads authoritative.
4. [x] Add unit tests for state mapping and idempotent no-op cases.
5. [x] Keep any live Stripe API/session evidence deferred to the Stripe access gate.

## Verification

- [x] `ReadCheckoutState` and webhook handling share state normalization
- [x] only verified webhook paths are allowed to perform authoritative transitions
- [x] app-owned order states remain stable
- [x] tests use fixtures/mocks and do not require Stripe account credentials
- [x] `pnpm test:unit`, `pnpm check`, and `pnpm build` pass

## Explicit Exclusions

- No stock decrement
- No shipping behavior
- No frontend UI changes
- No production webhook setup
- No live Stripe API validation
