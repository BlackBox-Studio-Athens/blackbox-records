---
phase: 08-webhook-orders-and-inventory
plan: 04
type: execute
wave: 2
depends_on: ['08-02', '08-03']
requirements: [ORDR-02, ORDR-04]
---

# 08-04 Plan - Add Shared Stripe Checkout Reconciliation Use Case

## Objective

Create one backend reconciliation use case that normalizes Stripe Checkout Session state for both webhook handling and `ReadCheckoutState`.

## Scope Boundary

This commit adds reconciliation logic but does not yet perform stock decrement.

## Tasks

1. Add a business-named reconciliation use case for Stripe Checkout Session state.
2. Map paid, unpaid, expired, open, and needs-review outcomes into app-owned order states.
3. Reuse the normalization path from `ReadCheckoutState` without making browser reads authoritative.
4. Add unit tests for state mapping and idempotent no-op cases.

## Verification

- `ReadCheckoutState` and webhook handling share state normalization
- only verified webhook paths are allowed to perform authoritative transitions
- app-owned order states remain stable
- `pnpm test:unit`, `pnpm check`, and `pnpm build` pass

## Explicit Exclusions

- No stock decrement
- No shipping behavior
- No frontend UI changes
- No production webhook setup
