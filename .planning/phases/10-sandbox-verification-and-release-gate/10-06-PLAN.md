---
phase: 10-sandbox-verification-and-release-gate
plan: 10-06
title: Fix Hosted Checkout Return Base Path And Buyer Success Page
status: complete
---

# 10-06 Plan - Fix Hosted Checkout Return Base Path And Buyer Success Page

## Goal

Fix the hosted Stripe Checkout return path for GitHub Pages sandbox UAT and make the return route feel like a buyer-facing order status page.

## Scope

- Preserve GitHub Pages `/blackbox-records` in Stripe Checkout success and cancel URLs.
- Keep webhook-confirmed Worker state as the only paid-order authority.
- Improve return page copy for paid, processing, open, expired, missing-session, and error states.
- Keep this sandbox UAT hardening separate from production go-live approval.

## Acceptance

- GitHub Pages payments return to `/blackbox-records/store/<slug>/checkout/return?session_id=...`, not `/store/<slug>/...`.
- The return page renders shopper copy such as `Order Confirmed`, not implementation copy such as `Checkout State`.
- Paid state is read from the Worker using the Stripe Checkout Session ID.
- `pnpm test:unit`, `pnpm check`, `pnpm build`, sandbox Worker deploy, and hosted sandbox smoke pass.

## Validation Notes

- Reported bad URL: `https://blackbox-studio-athens.github.io/store/disintegration-black-vinyl-lp/checkout/return?...`
  reached GitHub Pages 404 because the Worker fallback return URL lost the `/blackbox-records` base path.
- Fixed hosted smoke run: `20260517123136`.
- Fixed return evidence: Stripe returned to
  `https://blackbox-studio-athens.github.io/blackbox-records/store/disintegration-black-vinyl-lp/checkout/return/?session_id=...`.
- Browser Use blocker: Agentify navigation returned `missing_electron_binary`; DevTools/Playwright fallback evidence was
  used for hosted checkout validation.
- Validation: `pnpm test:unit`, `pnpm check`, `pnpm build`, `pnpm deploy:backend:sandbox`, and hosted sandbox smoke passed.
