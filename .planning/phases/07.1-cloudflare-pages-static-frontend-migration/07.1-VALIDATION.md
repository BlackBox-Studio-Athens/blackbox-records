# Phase 7.1 Validation

## 07.1-01 Cloudflare Pages Contract

- Result: passed.
- Evidence: documented the Cloudflare Pages static artifact boundary, rollback posture, Worker separation, public-only Pages environment variables, and non-secret project/CI naming in `07.1-CLOUDFLARE-PAGES-CONTRACT.md`.
- GitHub Pages remains the active deployment and rollback reference until Phase `07.1-05`.
- No runtime files, workflow files, DNS, Pages Functions, secrets, app behavior, or account-specific values changed.
- Validation: `git diff --check`; `pnpm check`.

## 07.1-02 Cloudflare Pages Direct Upload CI

- Result: passed locally; hosted deploy evidence remains for `07.1-04`.
- Evidence: added `.github/workflows/cloudflare-pages.yml` for Cloudflare Pages Direct Upload after `pnpm test:unit`, `pnpm check`, and `pnpm build`.
- The workflow uploads only `apps/web/dist` to the `blackbox-records-web` Pages project and keeps `.github/workflows/pages.yml` intact as the GitHub Pages rollback path.
- No Pages Functions, SSR, Worker deploy changes, DNS changes, secrets, D1 changes, Stripe changes, frontend route changes, or generated API client changes were introduced.
- Validation: `git diff --check`; `pnpm test:unit`; `pnpm check`; `pnpm build`.

## 07.1-03 Cloudflare Pages Env And Return Origins

- Result: passed locally; hosted origin validation remains for `07.1-04`.
- Evidence: wired `.github/workflows/cloudflare-pages.yml` to pass only `PUBLIC_BACKEND_BASE_URL` and `PUBLIC_STRIPE_PUBLISHABLE_KEY` into the static Astro build.
- Evidence: added `https://blackbox-records-web.pages.dev` to non-production Worker checkout return-origin allowlists while keeping local and GitHub Pages rollback origins.
- `PUBLIC_CHECKOUT_CLIENT_MODE` remains unset in the production Pages workflow; mock mode stays deliberate and non-production only.
- No wildcard Pages origins, Pages Functions, SSR, Worker route changes, secrets, D1 changes, Stripe gateway changes, frontend route changes, or generated API client changes were introduced.
- Validation: `git diff --check`; `pnpm --filter @blackbox/backend exec vitest run test/http/public-commerce-routes.test.ts test/http/app.test.ts`; `pnpm test:unit`; `pnpm check`; `pnpm build`.

## Phase-Level Acceptance

- Cloudflare Pages deploys `apps/web/dist` from CI after `pnpm test:unit`, `pnpm check`, and `pnpm build`.
- The static frontend remains Astro static output; no SSR, Pages Functions, or frontend-owned backend logic is introduced.
- `PUBLIC_BACKEND_BASE_URL` points to the intended Worker backend for the target environment.
- `PUBLIC_STRIPE_PUBLISHABLE_KEY` is configured only as browser-safe Pages environment configuration.
- Worker `CHECKOUT_RETURN_ORIGINS` includes only the intended Cloudflare Pages origins and local dev origins.
- Storefront, checkout shell, checkout return/retry UI, hidden stock UI route generation, sitemap, and 404 behavior work on Cloudflare Pages.
- GitHub Pages remains available as rollback until final acceptance, then docs identify Cloudflare Pages as canonical.

## Required Checks

- `pnpm test:unit`
- `pnpm check`
- `pnpm build`
- Cloudflare Pages preview deploy from a non-production branch
- Cloudflare Pages production-branch deploy
- Browser smoke on:
  - `/`
  - `/store/`
  - `/store/disintegration-black-vinyl-lp/`
  - `/store/disintegration-black-vinyl-lp/checkout/`
  - legacy `/store/barren-point/` redirect
  - `/stock/` behind the intended protected-hostname model
  - `/404.html`

## Explicit Non-Goals

- No production live-mode Stripe cutover.
- No Worker backend consolidation into Pages Functions.
- No D1 schema changes.
- No webhook/order/shipping behavior changes.
- No custom-domain DNS cutover before preview and production branch evidence exists.
