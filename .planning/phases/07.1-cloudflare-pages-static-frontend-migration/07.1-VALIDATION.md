# Phase 7.1 Validation

## 07.1-01 Cloudflare Pages Contract

- Result: passed.
- Evidence: documented the Cloudflare Pages static artifact boundary, rollback posture, Worker separation, public-only Pages environment variables, and non-secret project/CI naming in `07.1-CLOUDFLARE-PAGES-CONTRACT.md`.
- GitHub Pages remains the active deployment and rollback reference until Phase `07.1-05`.
- No runtime files, workflow files, DNS, Pages Functions, secrets, app behavior, or account-specific values changed.
- Validation: `git diff --check`; `pnpm check`.

## 07.1-02 Cloudflare Pages Direct Upload CI

- Result: passed locally; hosted deploy evidence remains for `07.1-04`.
- Evidence: added `.github/workflows/cloudflare-pages.yml` for Cloudflare Pages Direct Upload after `pnpm test:unit`, `pnpm check`, and `pnpm build`.
- The workflow uploads only `apps/web/dist` to the `blackbox-records-web` Pages project and keeps `.github/workflows/pages.yml` intact as the GitHub Pages rollback path.
- No Pages Functions, SSR, Worker deploy changes, DNS changes, secrets, D1 changes, Stripe changes, frontend route changes, or generated API client changes were introduced.
- Validation: `git diff --check`; `pnpm test:unit`; `pnpm check`; `pnpm build`.

## 07.1-03 Cloudflare Pages Env And Return Origins

- Result: passed locally; hosted origin validation remains for `07.1-04`.
- Evidence: wired `.github/workflows/cloudflare-pages.yml` to pass only `PUBLIC_BACKEND_BASE_URL` and `PUBLIC_STRIPE_PUBLISHABLE_KEY` into the static Astro build.
- Evidence: added `https://blackbox-records-web.pages.dev` to non-production Worker checkout return-origin allowlists while keeping local and GitHub Pages rollback origins.
- `PUBLIC_CHECKOUT_CLIENT_MODE` remains unset in the production Pages workflow; mock mode stays deliberate and non-production only.
- No wildcard Pages origins, Pages Functions, SSR, Worker route changes, secrets, D1 changes, Stripe gateway changes, frontend route changes, or generated API client changes were introduced.
- Validation: `git diff --check`; `pnpm --filter @blackbox/backend exec vitest run test/http/public-commerce-routes.test.ts test/http/app.test.ts`; `pnpm test:unit`; `pnpm check`; `pnpm build`.

## 07.1-04 Cloudflare Pages Validation Gate

- Result: in progress; do not mark `07.1-04` complete until GitHub Actions preview and production deployments render correctly and Browser Use hosted smoke evidence exists.
- Evidence: pushed `main` so the Cloudflare Pages workflow exists remotely.
- Evidence: hosted run `25045507989` failed before deploy because `@blackbox/api-client` did not declare the Node type package required by its TypeScript config. Fixed in commit `ae270b7`.
- Evidence: hosted run `25045722915` passed install, tests, checks, and build, then failed at Pages upload because `cloudflare/wrangler-action@v3` tried to install Wrangler into the workspace root. Fixed in commit `503dad5` by using the backend workspace Wrangler binary.
- Evidence: hosted run `25045878823` passed install, unit tests, workspace checks, and static frontend build, then failed during `wrangler pages deploy` with Cloudflare API authentication errors for `/accounts/2004bfa6f5ad8b48008f1243b195ab61/pages/projects/blackbox-records-web`.
- Evidence: GitHub repository variables currently include `CLOUDFLARE_ACCOUNT_ID`, but not `PUBLIC_BACKEND_BASE_URL` or `PUBLIC_STRIPE_PUBLISHABLE_KEY`.
- Evidence: sandbox Worker deploy was attempted. D1 `blackbox-records-commerce-sandbox` was provisioned and Worker deployment `aa7ac998-d91f-4d71-ac5f-0fb397d08afd` exists, but publish did not produce a usable workers.dev URL because the Cloudflare account has no registered workers.dev subdomain.
- Browser Use hosted smoke was not run because there is no validated Pages deployment URL with a configured Worker backend and browser-safe Stripe publishable key.
- 2026-04-29 evidence: the account now has the usable sandbox Worker hostname `https://blackbox-records-backend-sandbox.blackboxrecordsathens.workers.dev`.
- 2026-04-29 evidence: `wrangler pages project create blackbox-records-web --production-branch main` created the Direct Upload Pages project.
- 2026-04-29 evidence: `pnpm deploy:backend:sandbox` deployed Worker version `ba4607b9-3f91-4a67-9f0a-79e236224bc6`, then version `107796e3-8a0d-4318-98a7-287432bd1cd6` after adding the exact preview origin.
- 2026-04-29 evidence: GitHub repository variable `PUBLIC_BACKEND_BASE_URL` is now set to `https://blackbox-records-backend-sandbox.blackboxrecordsathens.workers.dev`.
- 2026-04-29 evidence: `pnpm build` passed with `PUBLIC_BACKEND_BASE_URL` pointing at the sandbox Worker and no Stripe publishable key configured.
- 2026-04-29 diagnostic evidence: direct local `wrangler pages deploy` created production and preview deployments, but those are no longer treated as acceptance evidence because Phase 7.1 requires GitHub Actions deployment.
- 2026-04-29 diagnostic evidence: the direct preview alias `https://codex-pages-no-stripe-valida.blackbox-records-web.pages.dev` rendered incorrectly because the static artifact was still built with the GitHub Pages base path `/blackbox-records/`, causing Cloudflare-root asset URLs to point at missing `/blackbox-records/_astro/*` files.
- 2026-04-29 correction: Cloudflare Pages builds must set `ASTRO_SITE_URL=https://blackbox-records-web.pages.dev` and `ASTRO_BASE_PATH=/`; GitHub Pages rollback keeps `https://blackbox-studio-athens.github.io` and `/blackbox-records/`.
- 2026-04-29 correction: the preferred validation preview branch is now `pages/no-stripe-validation`, not `codex/pages-no-stripe-validation`. Add only the exact emitted preview origin after the GitHub Actions run; do not guess or wildcard `*.pages.dev`.
- 2026-04-29 evidence: hosted run `25104284559` failed in unit tests because `PUBLIC_BACKEND_BASE_URL` was set at workflow job scope, contaminating tests that intentionally verify unset same-origin behavior. The workflow now scopes Cloudflare Pages public env to the build step only.
- 2026-04-29 evidence: remote D1 migrations `0001_initial_commerce_state.sql`, `0002_add_internal_stock_ledger.sql`, and `0003_add_checkout_order_lifecycle.sql` were applied to sandbox D1 `blackbox-records-commerce-sandbox` using an ephemeral local Wrangler config with the account-specific database id; the id was not committed.
- Required next action: deploy the corrected root-base artifact through `.github/workflows/cloudflare-pages.yml` on `main` and `pages/no-stripe-validation`, capture the exact preview origin, update non-production Worker return origins if checkout reads require it, and run Browser Use hosted smoke.
- Stripe next action remains deferred: configure a real test `PUBLIC_STRIPE_PUBLISHABLE_KEY`, real Stripe backend secrets/mappings, and then run hosted checkout validation. Do not seed fake local stock or `price_mock_*` rows into sandbox as a substitute for Stripe account readiness.
- Cloudflare Access + Google protection for the operator hostname remains deferred until the protected operator hostname/domain setup phase; it is not required for public Pages/Worker sandbox browsing.

## Phase-Level Acceptance

- Cloudflare Pages deploys `apps/web/dist` from GitHub Actions after `pnpm test:unit`, `pnpm check`, and `pnpm build`.
- The static frontend remains Astro static output; no SSR, Pages Functions, or frontend-owned backend logic is introduced.
- `PUBLIC_BACKEND_BASE_URL` points to the intended Worker backend for the target environment.
- `PUBLIC_STRIPE_PUBLISHABLE_KEY` is configured only as browser-safe Pages environment configuration.
- Worker `CHECKOUT_RETURN_ORIGINS` includes only the intended Cloudflare Pages origins and local dev origins.
- Storefront, checkout shell, checkout return/retry UI, hidden stock UI route generation, sitemap, and 404 behavior work on Cloudflare Pages.
- GitHub Pages remains available as rollback until final acceptance, then docs identify Cloudflare Pages as canonical.

## Required Checks

- `pnpm test:unit`
- `pnpm check`
- `pnpm build`
- Cloudflare Pages preview deploy from `pages/no-stripe-validation` through GitHub Actions
- Cloudflare Pages production-branch deploy from `main` through GitHub Actions
- Browser smoke on:
  - `/`
  - `/store/`
  - `/store/disintegration-black-vinyl-lp/`
  - `/store/disintegration-black-vinyl-lp/checkout/`
  - legacy `/store/barren-point/` redirect
  - `/stock/` behind the intended protected-hostname model
  - `/404.html`

## Explicit Non-Goals

- No production live-mode Stripe cutover.
- No Worker backend consolidation into Pages Functions.
- No D1 schema changes.
- No webhook/order/shipping behavior changes.
- No custom-domain DNS cutover before preview and production branch evidence exists.
