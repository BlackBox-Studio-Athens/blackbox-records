---
phase: 01-runtime-and-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/adrs/ADR-001-hosting-runtime.md, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [DEPL-01, DEPL-02]
user_setup: []
must_haves:
  truths:
    - "ADR-001 states that the native commerce runtime baseline is Astro on Cloudflare Workers, not GitHub Pages or a split-service fallback."
    - "Project requirements state that runtime secrets live in Worker secrets/bindings and that the commerce runtime supports server routes and webhooks."
  artifacts:
    - path: ".planning/adrs/ADR-001-hosting-runtime.md"
      provides: "Approved hosting and runtime baseline"
      contains: "Cloudflare Workers"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Normalized deployment and runtime requirements"
      contains: "Cloudflare Workers"
  key_links:
    - from: ".planning/adrs/ADR-001-hosting-runtime.md"
      to: ".planning/REQUIREMENTS.md"
      via: "shared Cloudflare Workers and runtime secret language"
      pattern: "Cloudflare Workers|runtime secrets"
---

<objective>
Lock the hosting/runtime ADR to the Cloudflare Workers direction and normalize the deployment requirements so later phases do not plan against stale GitHub Pages or generic-host assumptions.

Purpose: Make the runtime choice explicit enough that later Stripe, D1, and shipping plans cannot drift back to the old static-only or split-service model.
Output: Updated ADR-001 and deployment/runtime requirements aligned to Astro on Cloudflare Workers with server-only secret handling.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-and-guardrails/01-CONTEXT.md
@.planning/phases/01-runtime-and-guardrails/01-RESEARCH.md
@.planning/adrs/ADR-001-hosting-runtime.md
@.planning/REQUIREMENTS.md
</context>

<threat_model>
<trust_boundaries>
- Browser and static assets must not gain access to runtime-only secrets or privileged commerce operations.
- Cloudflare Worker server routes are the only allowed execution tier for Checkout Session creation and webhook handling.
- GitHub Actions may hold deploy credentials, but deployed runtime secrets must terminate as Worker secrets/bindings.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-01-01-A</threat_id>
  <category>Spoofing</category>
  <component>Runtime decision records</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Rewrite ADR-001 so the final runtime is unambiguous and no alternate runtime path remains implied for v1.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-01-01-B</threat_id>
  <category>Information Disclosure</category>
  <component>Deployment requirements</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Update requirements to state that runtime secrets belong in Worker secrets/bindings and are never browser-accessible.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ADR-001 around the Cloudflare Workers baseline</name>
  <files>.planning/adrs/ADR-001-hosting-runtime.md</files>
  <read_first>.planning/adrs/ADR-001-hosting-runtime.md, .planning/phases/01-runtime-and-guardrails/01-CONTEXT.md, .planning/phases/01-runtime-and-guardrails/01-RESEARCH.md</read_first>
  <action>Replace the current "portable Node host" recommendation in `ADR-001-hosting-runtime.md` with the Phase 1 locked decision: Astro deploys on Cloudflare Workers using the official Cloudflare adapter, brochure/content routes stay prerendered where practical, and commerce endpoints use on-demand Worker execution. Explicitly reject GitHub Pages as the final native-commerce host, reject a split Pages-plus-separate-API architecture for v1, and reject Java/Oracle/container baselines for this milestone. Keep the document as an ADR, but change its status and consequences so downstream planning treats Cloudflare Workers as the approved runtime baseline.</action>
  <verify>rg -n "Cloudflare Workers|official Cloudflare adapter|GitHub Pages|split.*API|Java|Oracle" .planning/adrs/ADR-001-hosting-runtime.md</verify>
  <acceptance_criteria>
    - `.planning/adrs/ADR-001-hosting-runtime.md` contains `Cloudflare Workers`
    - `.planning/adrs/ADR-001-hosting-runtime.md` contains `official Cloudflare adapter`
    - `.planning/adrs/ADR-001-hosting-runtime.md` contains `GitHub Pages` in a rejected or insufficient context, not as the approved final host
    - `.planning/adrs/ADR-001-hosting-runtime.md` contains `Java` and `Oracle` in rejected or out-of-scope context
  </acceptance_criteria>
  <done>ADR-001 is aligned to the locked runtime decision and no longer recommends a generic Node-host path.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize deployment requirements to Worker runtime language</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-runtime-and-guardrails/01-CONTEXT.md, .planning/phases/01-runtime-and-guardrails/01-RESEARCH.md</read_first>
  <action>Update `REQUIREMENTS.md` so the deployment/runtime section reflects the approved Worker baseline instead of vague hosting language. `DEPL-01` should explicitly require Astro storefront deployment on a runtime that supports on-demand server routes and webhooks while keeping brochure/content routes prerendered where practical, with Cloudflare Workers named as the chosen target. `DEPL-02` should replace `Supabase service-role credentials` with Worker-secret and D1-binding language, making clear that runtime secrets belong to Cloudflare Worker secrets/bindings even if GitHub Secrets are used for CI or deploy setup. `DEPL-03` should stop implying a Fourthwall fallback path and instead require an explicit deployment-level rollback and kill-switch model for native commerce cutover.</action>
  <verify>rg -n "DEPL-01|DEPL-02|DEPL-03|Cloudflare Workers|runtime secrets|D1|kill-switch|Fourthwall" .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `Cloudflare Workers` in `DEPL-01`
    - `.planning/REQUIREMENTS.md` contains `runtime secrets` or equivalent Worker-secret language in `DEPL-02`
    - `.planning/REQUIREMENTS.md` contains `D1` in deployment/runtime credential context instead of `Supabase service-role`
    - `.planning/REQUIREMENTS.md` defines rollback for `DEPL-03` without requiring a Fourthwall fallback path
  </acceptance_criteria>
  <done>Deployment requirements match the Cloudflare Workers runtime and secret-boundary decision.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Cloudflare Workers|official Cloudflare adapter|GitHub Pages" .planning/adrs/ADR-001-hosting-runtime.md` returns expected lines
- [ ] `rg -n "DEPL-01|DEPL-02|DEPL-03|D1|runtime secrets|kill-switch" .planning/REQUIREMENTS.md` returns expected lines
- [ ] `pnpm check` passes
</verification>

<success_criteria>

- All tasks completed
- ADR-001 names Cloudflare Workers as the approved runtime baseline
- Requirements no longer imply GitHub Pages or a Fourthwall fallback as the native-commerce endpoint
- Deployment requirements explicitly distinguish CI secrets from runtime Worker secrets
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-and-guardrails/01-01-SUMMARY.md`
</output>
