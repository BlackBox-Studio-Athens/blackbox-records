---
phase: 05-cloudflare-runtime-and-secret-plumbing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [wrangler.jsonc, .gitignore, .dev.vars.example, prisma/schema.prisma, prisma.config.ts]
autonomous: true
requirements: [DEPL-02, SECU-01]
user_setup:
  - "Cloudflare account access must be available locally or in CI before remote beta D1 creation or remote deploy verification can succeed."
must_haves:
  truths:
    - "Worker bindings for D1 and required secrets are declared in config rather than left implicit."
    - "Prisma is scaffolded for runtime access on D1 without front-running the actual commerce schema phase."
    - "Local secret and generated-client hygiene are explicit and safe for the repo."
  artifacts:
    - path: "wrangler.jsonc"
      provides: "Worker bindings, compatibility settings, and environment contract"
      contains: "d1_databases"
    - path: "prisma/schema.prisma"
      provides: "Prisma schema scaffold for D1 runtime access"
      contains: "runtime  = \"cloudflare\""
    - path: "prisma.config.ts"
      provides: "Prisma CLI config aligned to the current Prisma major"
      contains: "defineConfig"
  key_links:
    - from: "wrangler.jsonc"
      to: "prisma/schema.prisma"
      via: "shared D1 binding and Prisma-on-D1 contract"
      pattern: "DB|sqlite|cloudflare"
---

<objective>
Define the Worker binding contract and scaffold the Prisma-on-D1 workflow without introducing the actual order or inventory schema yet.

Purpose: Give later phases a safe and maintainable runtime foundation for D1 access and migrations while keeping secrets server-only.
Output: `wrangler.jsonc`, Prisma-on-D1 scaffold files, and local-secret hygiene files that match the current official D1 + Prisma workflow.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md
@.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md
@.gitignore
</context>

<threat_model>
<trust_boundaries>
- Runtime secrets must terminate inside the Worker runtime and never in browser code or committed files.
- D1 access should enter the codebase through a maintainable Prisma workflow, not ad hoc direct calls everywhere.
- Phase 5 must not invent the production schema early; it should only scaffold the workflow and environment contract.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-05-02-A</threat_id>
  <category>Information Disclosure</category>
  <component>Worker bindings and secrets</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Declare D1 bindings and required secret names in `wrangler.jsonc`, plus local example files, so secrets do not leak into source control or browser code.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-05-02-B</threat_id>
  <category>Tampering</category>
  <component>Database access workflow</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Scaffold Prisma-on-D1 using the documented adapter and migration flow, while deferring actual domain models and migrations to Phase 8.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add wrangler.jsonc with D1 bindings and required secret names</name>
  <files>wrangler.jsonc</files>
  <read_first>.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md, astro.config.mjs</read_first>
  <action>Create `wrangler.jsonc` for the Worker runtime. Include the Worker entry/output shape expected by Astro, static assets binding, compatibility date, required compatibility flags needed by the current Astro Cloudflare docs, and a beta D1 binding contract. Use the new `secrets.required` property so local development and deploys fail early when required Stripe secrets are missing. Add an explicit beta environment section and keep future production D1 support as a later environment extension, not as a shared database. Use a stable Worker name/path that maps cleanly to the dedicated sandbox deployment workflow.</action>
  <verify>rg -n "main|assets|d1_databases|secrets|required|STRIPE_SECRET_KEY|STRIPE_WEBHOOK_SECRET|env.beta" wrangler.jsonc</verify>
  <acceptance_criteria>
    - `wrangler.jsonc` contains `d1_databases`
    - `wrangler.jsonc` contains `secrets.required`
    - `wrangler.jsonc` declares the Stripe secret names required for sandbox checkout and webhook work
    - `wrangler.jsonc` includes an explicit beta environment contract
  </acceptance_criteria>
  <done>The Worker runtime has an explicit binding and secret contract for local and beta sandbox work.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Prisma-on-D1 and local secret hygiene</name>
  <files>prisma/schema.prisma, prisma.config.ts, .dev.vars.example, .gitignore</files>
  <read_first>.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md, .gitignore</read_first>
  <action>Add `prisma/schema.prisma` with the current Prisma D1 shape: `sqlite` datasource and `cloudflare` runtime generator output. Add `prisma.config.ts` if required by the current Prisma major so Prisma CLI commands follow current docs. Do not introduce actual commerce models yet; keep the schema scaffold intentionally minimal and document that real order/inventory models belong to Phase 8. Add `.dev.vars.example` with the required secret names and update `.gitignore` so `.dev.vars`, generated Prisma output, and any other local-only Worker or Prisma artifacts are ignored safely.</action>
  <verify>rg -n "provider *= *\"sqlite\"|runtime *= *\"cloudflare\"|defineConfig|STRIPE_SECRET_KEY|STRIPE_WEBHOOK_SECRET|\\.dev\\.vars|src/generated/prisma" prisma/schema.prisma prisma.config.ts .dev.vars.example .gitignore</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains a `sqlite` datasource
    - `prisma/schema.prisma` contains a Prisma generator with `runtime = "cloudflare"`
    - `prisma.config.ts` exists and matches the current Prisma CLI contract
    - `.dev.vars.example` documents the required local secret names
    - `.gitignore` ignores `.dev.vars` and generated Prisma output
  </acceptance_criteria>
  <done>The repo now has a safe Prisma-on-D1 scaffold and local secret hygiene without prematurely implementing the commerce schema.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "d1_databases|secrets|required|STRIPE_SECRET_KEY|STRIPE_WEBHOOK_SECRET|env.beta" wrangler.jsonc` returns expected lines
- [ ] `rg -n "provider *= *\"sqlite\"|runtime *= *\"cloudflare\"|defineConfig|\\.dev\\.vars" prisma/schema.prisma prisma.config.ts .dev.vars.example .gitignore` returns expected lines
- [ ] `pnpm check` passes
</verification>

<success_criteria>

- All tasks completed
- Worker secret and D1 binding contracts are explicit and checked in
- Prisma is set up for runtime readability without violating the D1 migration model
- Local secret files and generated artifacts are safely ignored
</success_criteria>

<output>
After completion, create `.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-02-SUMMARY.md`
</output>
