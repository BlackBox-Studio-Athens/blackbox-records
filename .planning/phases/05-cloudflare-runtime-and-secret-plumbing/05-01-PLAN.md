---
phase: 05-cloudflare-runtime-and-secret-plumbing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, astro.config.mjs]
requirements: [DEPL-01]
---

# 05-01 Plan - Astro Cloudflare Adapter Bootstrap

## Objective

Add the Cloudflare adapter and Worker-first script surface without removing the legacy Pages build path.

## Tasks

1. Add the Cloudflare runtime dependencies and any supporting Worker toolchain packages.
2. Make `pnpm build` and local dev align to the Worker-first alpha path.
3. Preserve one explicit Pages-compatible build path so the brownfield baseline is not silently lost.

## Verification

- `package.json` includes Worker runtime tooling
- `astro.config.mjs` is Worker-capable
- the repo still has an explicit Pages-compatible build path
