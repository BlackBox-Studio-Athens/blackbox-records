---
phase: 05-cloudflare-runtime-and-secret-plumbing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, astro.config.mjs]
autonomous: true
requirements: [DEPL-01]
user_setup: []
must_haves:
  truths:
    - "The repo gains the Cloudflare Worker runtime toolchain and the Worker build becomes the active alpha path."
    - "The Astro config remains a single file and keeps brochure routes prerendered by default."
    - "The legacy Pages build remains available through an explicit non-default build script so the brownfield baseline is not silently lost."
  artifacts:
    - path: "package.json"
      provides: "Worker, Wrangler, and Prisma toolchain plus explicit build scripts"
      contains: "@astrojs/cloudflare"
    - path: "astro.config.mjs"
      provides: "Single Astro config with Worker-first runtime logic"
      contains: "@astrojs/cloudflare"
  key_links:
    - from: "package.json"
      to: "astro.config.mjs"
      via: "shared Worker-first build target and legacy Pages fallback script"
      pattern: "build:worker|build:pages|cloudflare"
---

<objective>
Add the Cloudflare Worker runtime toolchain and convert the Astro config to a Worker-first setup without deleting the legacy Pages build path.

Purpose: Make the repo capable of real server execution for later checkout and webhook phases while preserving an explicit baseline build for the current Pages deployment.
Output: Updated `package.json` and `astro.config.mjs` that support Worker builds, local Worker development, and a legacy Pages build mode.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md
@.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md
@package.json
@astro.config.mjs
</context>

<threat_model>
<trust_boundaries>
- Worker runtime code must be able to execute server-only logic later without exposing secrets to the browser.
- The legacy Pages build path must remain available during the milestone even though Worker builds become the active alpha target.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-05-01-A</threat_id>
  <category>Denial of Service</category>
  <component>Build/runtime configuration</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Add the Worker runtime and scripts without removing the ability to produce the current Pages build as an explicit fallback build target.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-05-01-B</threat_id>
  <category>Tampering</category>
  <component>Astro config</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep one Astro config and make the Worker path explicit so later phases cannot drift back to a purely static-only assumption.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the Worker and Prisma toolchain to package.json</name>
  <files>package.json</files>
  <read_first>package.json, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md</read_first>
  <action>Update `package.json` to add the Astro Cloudflare adapter, Wrangler, Prisma CLI, Prisma Client, and the D1 adapter required by the current official D1 workflow. Add explicit scripts for Worker-first development and build flows, plus an explicit legacy Pages build script. Keep `pnpm build` aligned to the active Worker-first alpha path, and add a `build:pages` script for the brownfield baseline. Add any supporting scripts needed for `wrangler types` or Prisma client generation if the chosen Prisma major expects them.</action>
  <verify>rg -n "@astrojs/cloudflare|wrangler|prisma|@prisma/client|@prisma/adapter-d1|build:pages|build:worker|dev:worker" package.json</verify>
  <acceptance_criteria>
    - `package.json` contains `@astrojs/cloudflare`
    - `package.json` contains `wrangler`
    - `package.json` contains `prisma`, `@prisma/client`, and `@prisma/adapter-d1`
    - `package.json` defines an explicit Worker build script and a separate `build:pages` script
  </acceptance_criteria>
  <done>The repo has the runtime packages and scripts needed for Worker-first execution without erasing the legacy Pages build path.</done>
</task>

<task type="auto">
  <name>Task 2: Convert astro.config.mjs to a Worker-first single-config setup</name>
  <files>astro.config.mjs</files>
  <read_first>astro.config.mjs, package.json, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md</read_first>
  <action>Update `astro.config.mjs` to use the Cloudflare adapter in a one-config setup. Keep brochure routes prerendered by default so later phases can opt only commerce routes into on-demand rendering. Make the Worker path the default alpha build target while preserving an explicit legacy Pages mode through environment switching or equivalent config branching inside the same file. Preserve React and Tailwind integration behavior and keep the current `site` / `base` values available for the legacy Pages build mode.</action>
  <verify>rg -n "@astrojs/cloudflare|defineConfig|output: 'static'|site:|base:|DEPLOY_TARGET|Pages" astro.config.mjs</verify>
  <acceptance_criteria>
    - `astro.config.mjs` imports and uses the Cloudflare adapter
    - `astro.config.mjs` keeps prerender-by-default behavior for non-commerce routes
    - `astro.config.mjs` preserves an explicit legacy Pages mode inside the same config file
    - React and Tailwind integration remain present
  </acceptance_criteria>
  <done>The Astro config now supports Worker-first alpha builds while retaining an explicit legacy Pages path in the same file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "@astrojs/cloudflare|wrangler|prisma|build:pages|build:worker" package.json` returns expected lines
- [ ] `rg -n "@astrojs/cloudflare|output: 'static'|site:|base:" astro.config.mjs` returns expected lines
- [ ] `pnpm check` passes
</verification>

<success_criteria>

- All tasks completed
- The repo can target Cloudflare Workers without losing an explicit Pages build path
- The Astro config remains a single source of truth
- Worker-first scripts exist for local dev and build
</success_criteria>

<output>
After completion, create `.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-01-SUMMARY.md`
</output>
