---
phase: 05-cloudflare-runtime-and-secret-plumbing
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified: [.github/workflows/cloudflare-sandbox.yml, README.md, AGENTS.md]
autonomous: true
requirements: [DEPL-03]
user_setup:
  - "Repository secrets `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` must be available in GitHub before sandbox CI deploys can succeed."
must_haves:
  truths:
    - "Sandbox Worker deploys are automated and isolated from the GitHub Pages workflow."
    - "The repo docs describe the Worker-first alpha path, the legacy Pages baseline, and the deferred Cloudflare Access posture."
    - "The deployment workflow uses the dedicated `sandbox` branch plus a manual dispatch override."
  artifacts:
    - path: ".github/workflows/cloudflare-sandbox.yml"
      provides: "Dedicated Worker sandbox deployment workflow"
      contains: "workflow_dispatch"
    - path: "README.md"
      provides: "Operator-facing setup and command guidance"
      contains: "Workers"
    - path: "AGENTS.md"
      provides: "Repo-local execution rules aligned to the new runtime model"
      contains: "Cloudflare Workers"
  key_links:
    - from: ".github/workflows/cloudflare-sandbox.yml"
      to: "README.md"
      via: "shared sandbox branch and manual-dispatch deployment guidance"
      pattern: "sandbox|workflow_dispatch|wrangler"
---

<objective>
Add the isolated sandbox deployment workflow and update repo guidance so the Worker runtime is operable without disturbing the legacy Pages workflow.

Purpose: Make Phase 5 operationally usable for later checkout work and keep the repo instructions aligned with the Worker-first alpha architecture.
Output: A dedicated Worker sandbox GitHub Actions workflow plus updated `README.md` and `AGENTS.md`.
</objective>

<execution_context>
@C:/Users/SVall/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/SVall/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md
@.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md
@README.md
@AGENTS.md
@.github/workflows/pages.yml
</context>

<threat_model>
<trust_boundaries>
- Sandbox Worker deploys must not be triggered by the GitHub Pages production workflow.
- Repo docs must not imply the sandbox is private while Cloudflare Access is deferred.
- Human operators need a documented way to run Worker builds locally and in CI without guessing script names or secret locations.
</trust_boundaries>
<stride>
<threat>
  <threat_id>T-05-03-A</threat_id>
  <category>Elevation of Privilege</category>
  <component>CI/CD workflow boundaries</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Create a dedicated Worker sandbox workflow that only triggers from `sandbox` or manual dispatch, leaving the Pages workflow unchanged.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-05-03-B</threat_id>
  <category>Repudiation</category>
  <component>Repo operating docs</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Update README and AGENTS so contributors understand the Worker-first alpha path, required CI secrets, and the fact that Cloudflare Access is deferred.</mitigation_plan>
</threat>
</stride>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated Worker sandbox GitHub Actions workflow</name>
  <files>.github/workflows/cloudflare-sandbox.yml</files>
  <read_first>.github/workflows/pages.yml, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md</read_first>
  <action>Create `.github/workflows/cloudflare-sandbox.yml` as a separate workflow from Pages. Trigger it automatically on pushes to the dedicated `sandbox` branch and also on `workflow_dispatch`. Use the official Cloudflare Wrangler GitHub Action with `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` secrets, and run the repo verification commands before deploy. Make sure the deploy command uses the Worker/beta environment path defined in Phase 5 and does not alter `.github/workflows/pages.yml` behavior.</action>
  <verify>rg -n "push:|sandbox|workflow_dispatch|wrangler-action|CLOUDFLARE_API_TOKEN|CLOUDFLARE_ACCOUNT_ID" .github/workflows/cloudflare-sandbox.yml</verify>
  <acceptance_criteria>
    - `.github/workflows/cloudflare-sandbox.yml` exists
    - The workflow triggers on pushes to `sandbox`
    - The workflow also supports `workflow_dispatch`
    - The workflow uses the official Wrangler GitHub Action
    - `.github/workflows/pages.yml` is not repurposed to deploy the Worker sandbox
  </acceptance_criteria>
  <done>The repo now has an isolated automated deploy path for the Worker sandbox.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and AGENTS for the Worker-first alpha runtime</name>
  <files>README.md, AGENTS.md</files>
  <read_first>README.md, AGENTS.md, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-CONTEXT.md, .planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-RESEARCH.md, .github/workflows/cloudflare-sandbox.yml</read_first>
  <action>Update `README.md` and `AGENTS.md` so they accurately describe the new runtime reality: Worker-first alpha sandbox path, explicit legacy Pages baseline, local Worker/D1/Prisma commands, and the dedicated sandbox deployment workflow. Document the Prisma-on-D1 migration rule clearly: Prisma runtime plus `prisma migrate diff`, with Wrangler applying migrations. Also document that Cloudflare Access is intentionally deferred, so the sandbox Worker is not a private environment during this milestone.</action>
  <verify>rg -n "Workers|D1|Prisma|migrate diff|build:pages|sandbox|workflow_dispatch|Cloudflare Access" README.md AGENTS.md</verify>
  <acceptance_criteria>
    - `README.md` describes the Worker-first alpha workflow and the legacy Pages baseline
    - `README.md` documents the new local/dev/build/deploy commands
    - `AGENTS.md` reflects the Worker runtime and Prisma-on-D1 migration model
    - Both docs state that Cloudflare Access is deferred and the sandbox is not strongly access-controlled
  </acceptance_criteria>
  <done>Repo-local operating docs match the new runtime and deployment model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "push:|sandbox|workflow_dispatch|wrangler-action" .github/workflows/cloudflare-sandbox.yml` returns expected lines
- [ ] `rg -n "Workers|D1|Prisma|migrate diff|build:pages|Cloudflare Access" README.md AGENTS.md` returns expected lines
- [ ] `pnpm test:unit && pnpm check && pnpm build && pnpm build:pages` passes
</verification>

<success_criteria>

- All tasks completed
- Sandbox Worker deploys are automated and isolated from Pages
- Repo docs accurately describe the new runtime and migration workflow
- The deferred Cloudflare Access posture is documented explicitly
</success_criteria>

<output>
After completion, create `.planning/phases/05-cloudflare-runtime-and-secret-plumbing/05-03-SUMMARY.md`
</output>
