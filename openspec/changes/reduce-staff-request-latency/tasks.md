# Tasks

Implement `reduce-staff-request-latency` in this order. [design.md](design.md) fixes the architecture, file map, image contract, and failure behavior. [acceptance.md](acceptance.md) contains commands and evidence fields. Read the two delta specs before coding. Each numbered section is a bounded work session; keep unrelated changes out of it. All paths below are relative to the repository root.

Sections 1–6 are local implementation. Sections 7–8 require their own hosted evidence and existing release authorization; do not treat "apply this plan" as blanket permission to deploy or run hosted batches. Use authorization already present in the implementation session rather than asking twice. Leave unfinished checks unchecked and report the local/hosted status separately. Do not add a router, paid service, cache TTL, dependency, database migration, or resource relocation.

## 1. Establish the local starting point

- [x] 1.1 Read the repository startup files, `docs/cloudflare-free-tier.md`, the current staff/editor validation instructions, and this change's artifacts; inspect the current working tree and related active staff changes. Verify by recording the starting revision and relevant local changes in the implementation evidence without modifying other work.
- [x] 1.2 Build the current staff bundle and inspect the existing hosting/browser fixtures named in design decision 7. Capture the current route dispatch, compact artwork requests, optional-feature requests, and Overview loading behavior locally. Verify that the retained hosted diagnosis is reused and no new hosted probe is needed for this baseline.

## 2. Serve authenticated static files directly

- [x] 2.1 Update entry routing in `apps/backend/src/cms/index.ts` exactly as design decision 1: reuse `authenticate`, call `ASSETS.fetch` directly for classified GET/HEAD staff files, retain `staffAssetResponse`, and remove the obsolete object static branch. Verify with a focused routing test that valid static requests invoke ASSETS once and invoke neither CMS_RUNTIME nor D1, while public/internal APIs, workflows, exports, and unsupported methods retain their current destinations.
- [x] 2.2 Extend the existing asset/auth tests and `apps/backend/test/emdash/staff-smoke.mjs` for authenticated GET/HEAD 200 and 304 plus missing/invalid identity, alternate hostname, and matching-validator denial. Verify private cache headers, empty HEAD/304 bodies, no session cookie, and authorization before the asset binding is called.
- [x] 2.3 Run `pnpm --filter @blackbox/backend build:cms` and `pnpm --filter @blackbox/backend test:staff-hosting`. Verify the actual generated deployment keeps `run_worker_first: true`, existing D1/DO/R2 bindings, no KV, and the private asset behavior from 2.2; retain compact results in the local evidence.

## 3. Add private bounded thumbnails and use them in compact rows

- [x] 3.1 Add `apps/backend/src/cms/staff-thumbnails.ts` with the fixed filename, R2 key, PNG, 96-pixel, 40-KiB, and metadata rules from design decision 2. Verify focused tests reject traversal/encoding tricks, unsupported names, invalid PNGs, excessive dimensions/bytes, and accept current native and hyphenated fixture keys.
- [x] 3.2 Route the exact private thumbnail prefix in the entry Worker using existing identity verification and editorial role checks. Verify GET makes one R2 GET, HEAD one R2 HEAD, neither reaches CMS/D1 or writes storage, token export cannot access it, and every success/error has the specified private/no-store response.
- [x] 3.3 Update `media-upload.ts` and the exact native media POST wrapper in `cms/index.ts` to reuse validated thumbnail data and persist only after native success using the returned storage key. Verify new upload 201, deduplicated 200, rejected upload, incompatible optional thumbnail, and derivative PUT failure; original status/body/identity and original validation must remain intact.
- [x] 3.4 In `apps/staff/src/lib/backend/editorial-api.ts`, resize the existing Canvas thumbnail to at most 96 pixels and add `staffThumbnailUrl` without changing `editorialMediaUrl`. Extend `editorial-api.test.ts` to verify supported media shapes, approved origin/path handling, malformed/foreign URLs, and empty fallback; verify upload output stays PNG within the bound.
- [x] 3.5 Switch compact row artwork in `content/ContentApp.tsx` and `stock/StockOperationsApp.tsx` to the new helper. Verify loaded images retain alt text and dimensions, failures show the existing placeholder without an original request or retry loop, and full editor/media previews still use their current originals.

## 4. Prepare existing derivatives without changing content

- [x] 4.1 Create `apps/backend/scripts/prepare-staff-thumbnails.mjs` using the installed `sharp`, Node arguments, and existing proxy/config cleanup pattern. Implement precisely the environment, one-page limit, 64-MiB budget, cursor, dry-run/apply, original ETag, and hosted-review rules in design decision 4. Verify `--help`/invalid arguments cannot connect to resources and Local dry run binds only MEDIA and makes zero PUTs.
- [x] 4.2 Add one focused preparation test file beside `apps/backend/test/emdash/cms-backup.test.mjs` and register it in the normal repository test gate. Verify derivative-only writes, output bounds/orientation, valid-existing skips, no writes during dry run, environment separation, non-image/published-prefix skips, ETag mismatch, byte limits, and replay after an interrupted page without advancing past unfinished work.
- [x] 4.3 Run the two Local commands in design decision 4 against disposable local fixtures, then rerun apply. Verify the JSON reports show zero dry-run writes, bounded first-apply operations, zero second-apply writes for prepared images, and unchanged original bytes/CMS records; retain the exact commands and reports in `.codex-artifacts/`.

## 5. Reduce initial dependencies and unblock Overview panels

- [x] 5.1 Move pure section labels/types from `content/ContentFields.tsx` to `apps/staff/src/lib/content-sections.ts` and update every importer listed in design decision 6. Verify type checks and existing label/collection tests pass and the metadata module imports no editor component or stylesheet; preserve the existing lazy `ContentBodyEditor`.
- [x] 5.2 Load `PublicationHistory` from `StaffShell.tsx` only when opened, using React lazy loading and existing loading/error/modal primitives. Verify built Overview/Stock/Orders requests exclude closed history/editor code and editor CSS, while opening history and an editor loads the correct feature and preserves focus/close/error behavior.
- [x] 5.3 Refactor only `StaffOverview.tsx` loading coordination so drafts, publication state, and orders settle independently with panel-specific error/retry state and obsolete-response protection. Verify delayed/failed orders cannot block successful other panels, an unmounted/older activation cannot overwrite current data, and existing read freshness remains unchanged.

## 6. Complete local acceptance and document operation

- [x] 6.1 Extend `scripts/test-content-workspace.mjs` rather than creating another harness: assert no original requests and at most 1 MiB of image bodies for 25 compact covers, stable missing-image placeholders, optional-feature request boundaries, and independent Overview states. Verify these assertions run against the built staff bundle and fail when fixtures intentionally violate the relevant contract.
- [x] 6.2 Run the Chromium and Firefox workspace commands in `acceptance.md` and the relevant local CMS content/render checks. Verify existing autosave, failed/conflicting save, leave/reload, stock recovery, media upload, and publication behavior still passes; do not substitute mocked browser checks for the built-Worker hosting check.
- [x] 6.3 Update `docs/content-workspace.md` with the derivative route/ownership, Local preparation commands, hosted budget requirement, missing-thumbnail behavior, and rollback notes. Verify the documented commands match implemented argument parsing and the account-operation worksheet includes derivative storage and backup overhead.
- [x] 6.4 Run `pnpm validate` and `pnpm validate:editor` against the final source. Inspect compact summaries and only relevant failure logs; fix failures and rerun affected gates. Verify both pass for the recorded fingerprint, all required local evidence is linked from `acceptance.md`, and the canonical Local stack remains usable. Do not mark local implementation complete on `validate:fast` alone.

## 7. Accept the UAT candidate and measure Greek latency

- [x] 7.1 Confirm existing UAT deployment authorization and complete the UAT usage/headroom worksheet using current account evidence and measured Local operation counts. Verify the allowed candidate deployment, thumbnail pilot, authentication checks, and at most 12 timing activations fit the budget; if authorization or evidence is unavailable, hand off the locally verified result and leave this section unchecked.
- [ ] 7.2 Deploy the canonical combined CMS UAT artifact through the current repository release path. Run the preparation dry run and explicit apply with `--limit 3` first, then only the further reviewed bounded batches needed for the acceptance sample. Verify actual R2 counts and storage growth against estimates, original data remains unchanged, and record the deployed revision and preparation reports.
- [ ] 7.3 Check hosted static bypass, private authorization/304 behavior, no sessions/KV, entry CPU, and compact image limits within the reviewed pilot. Verify results are tied to the deployed revision and that no quota warning is hidden by a retry or a paid setting; stop affected hosted work if usage exceeds its estimate.
- [ ] 7.4 Complete the real Greek-network worksheet: first visit plus three warm visits per route when budget permits, visible completion states, individual asset/API timings, median/range, and request/image counts. Verify the ≤500-ms warm HTML median and ≤1,500-ms usable-content target per route, and investigate repeated critical assets above two seconds. If a target fails, record the residual trace and required follow-up and keep performance acceptance open; do not change placement or the navigation architecture in response without a separate scope decision.

## 8. Promote and verify PRD only with its release authorization

- [ ] 8.1 Obtain or reuse explicit PRD promotion authorization for the reviewed candidate through the existing full-SHA/candidate-evidence process, and complete the separate PRD usage worksheet. Verify the authorization covers code promotion and the bounded derivative preparation only; it must not imply catalog mutation, launch approval, or a cloud-plan change.
- [ ] 8.2 Perform the authorized PRD derivative pilot/preparation and promote the reviewed combined staff/CMS artifact through the canonical release process. Verify compact covers are prepared for the acceptance sample, the deployed revision matches the reviewed candidate, and private access/cache/no-session behavior remains correct within the budget. Retain the previous artifact for rollback; leave unused derivative objects in place if rollback is required.
- [ ] 8.3 Record the final Local/UAT/Greek-performance/PRD statuses and evidence in `acceptance.md`, with any still-pending acceptance work explicit. Verify every checked task has its stated evidence and only archive when required acceptance is complete; never report the hosted latency issue fixed solely because code and Local tests passed.
