## 1. Confirm source and presentation contracts

- [ ] 1.1 Run `pnpm openspec:guard` and inspect current Store Item source identity, release/artist lookups, listen trigger and shell handling; record supported/no-provider/Distro examples and verify the approach adds no guessed relationship or new content schema.
- [ ] 1.2 Complete the applicable Impeccable review for the accepted Listen placement near title/artist before long copy, using the Releases treatment and restrained editorial links; preserve Add to Cart priority and coordinate the purchase hierarchy with purchase-information and Distro-gallery changes.

## 2. Reuse player and editorial navigation

- [ ] 2.1 Resolve source-release player data and real release/artist URLs at build time with existing helpers; verify focused fixtures for a supported provider, no provider, absent optional artist, and a missing required source without title-based fallback.
- [ ] 2.2 Render the existing standalone Listen trigger and verified editorial links on eligible Store Items; verify a release with no provider retains its valid release/artist links while omitting Listen, and Distro without explicit relations gains neither. Preserve existing selected-option clarity without adding a second purchase-summary block; verify cart/provider payloads remain unchanged.
- [ ] 2.3 Exercise session reuse and normal overlay navigation through existing shell wiring; verify one active iframe, same-release identity, provider switching where supported, pre-interaction Close, post-interaction Minimize, Stop, and restored focus without a page-local player.

## 3. Validate item and navigation behavior

- [ ] 3.1 Run `pnpm test:unit`, `pnpm check`, and `pnpm build`; confirm generated item links resolve under both deployment base paths and Store Item/checkout routes remain outside section-route classification.
- [ ] 3.2 Use native Browser Use on desktop/mobile and with reduced motion to verify Listen near item identity before long copy, restrained editorial links, Add to Cart priority, release/artist overlays, keyboard focus, player failure/dismissal, and independent purchase actions; include 390x844 and 1280x800 views and record supported same-document persistence and expected full-document termination without making a payment.
- [ ] 3.3 Run `pnpm openspec -- validate add-store-item-listening-context --type change --strict` and `git diff --check`; link focused and browser evidence before proposing archive, keeping any authorized deployment acceptance distinct from implementation checks.
