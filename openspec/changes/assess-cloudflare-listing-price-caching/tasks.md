## 1. Confirm prerequisite and existing freshness

- [ ] 1.1 Run `pnpm openspec:guard`, reuse accepted EmDash/runtime-publication receipts and reconcile current listing/freshness contracts; record source/deployed/snapshot identities, listing route and public/private binding boundaries without reopening cutover or requiring archive.
- [ ] 1.2 Inventory static assets, no-store runtime HTML, existing code/snapshot-keyed renderer cache and pointer interval, immutable accepted media, shell cache and Worker API Freshness. Verify listing remains one fresh read per activation and distinguish commerce snapshot updates from editorial publication without changing either policy.

## 2. Collect bounded evidence

- [ ] 2.1 Use existing observability to establish listing demand, bytes, locality, read/CPU/object cost and update frequency, separating renderer/R2 work. Reuse publication evidence only for its measured scope; report missing listing data as unknown rather than substituting purchase counts, publication timings or historical quota headroom.
- [ ] 2.2 Rehearse representative listing measurement locally across usable fixed/custom prices, missing/inactive snapshots and updates; verify counts/timings are reproducible and distinguish Local simulation from actual edge-cache evidence.
- [ ] 2.3 Trace GET side effects, read current account-wide resource headroom and set explicit request/operation caps before a UAT pilot of at most five listing reads with no automatic retries; deliver actual costs and stop if the budget or observed side effects differ from expectations.
- [ ] 2.4 Verify current Free-plan/hostname support for conditional responses, browser and public edge candidates; report locality/Access/CORS/key/invalidation/failure costs and reads saved. Preserve the existing service gateway/renderer boundaries and prove any validator covers commerce price updates independently of editorial snapshot identity.

## 3. Discuss and record the decision

- [ ] 3.1 Present the measured no-store-versus-cache comparison to the user with concrete proposed freshness windows only if justified; record the explicit decision and do not infer TTL approval from the original assessment request or elapsed time.
- [ ] 3.2 If no cache is justified, record the no-store decision and close the assessment; if a cache is agreed, use the OpenSpec update workflow or an approved implementation change to define exact TTL/mechanism, full affected listing/freshness deltas, budget and rollout before any runtime edit, verifying the resulting artifacts match the user's decision.
- [ ] 3.3 Validate assessment artifacts with strict OpenSpec and targeted formatting checks; if diagnostic code changes behavior, run its checks and `pnpm validate` against the final source fingerprint, plus applicable editor/publication checks if affected. Report limitations without claiming a cache was deployed or a TTL approved.
