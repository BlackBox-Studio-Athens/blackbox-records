# Measurement evidence

## Current result — 2026-09-10: Accepted with caveats

The implementation and browser acceptance are complete. Full unit tests, pnpm check, pnpm build, unused-code audit, commerce-boundary audit, bundle validation, and four affected changes' strict validation pass. On 2026-09-10 the user explicitly accepted this implementation with its recorded caveats and authorized closure. This is a change-specific acceptance exception, not a claim that every raw timing meets the original gate: mobile disclosure records 51 ms uninstrumented and 51–77 ms with tracing; wide traversal has occasional large low-CPU spans whose attribution remains inconclusive. Original budgets and raw failures remain intact for future work. The sections below this current result are chronological evidence, including rejected implementations and superseded decisions.

Final reports are retained under .codex-artifacts/runtime-performance/contain-closure/. Normal reports identify revision d994bb01264f473cfc31bf69be78f9d5e99e4a33, source aabe0aa90a6181749e707a2b74f152f2246b92c8ba8e33f492293d5f468a5e3c, build ef19f70a1c59c4173781624d98d0e433c1378bd2053f7adb228e15d5eb47e0a7, Chromium 151.0.7922.34, and unchanged inputs. Temporary disclosure tracing changed only the runner; it used the same application build and was restored afterward.

Five cold mobile runs per route pass: Store All LCP median/p75 1.072/1.080 seconds and maximum CLS 0.010066; Distro 1.164/1.188 seconds and CLS 0.000642. All three Store activations retain 104 cards, one listing projection, zero per-card Store Offer reads, and zero Store 5xx. The static projection 404 and its resource console message remain separately classified as local API unavailability. These are Local production-build checks, not hosted commerce acceptance.

Mobile expanded traversal has zero layout slices, application-work p95 at most 0.388 ms, paint at most 5.836 ms, and tasks at most 11.654 ms. Wide quiet confirmation has rendering slices below 13.6 ms; larger raw spans include an automation animation-frame callback with 144.545 ms wall duration but 0.188 ms thread CPU. Raw slow samples and matched controls remain retained in the primary and quiet reports and contain-closure-attribution.json. Thread CPU is diagnostic attribution evidence, not a replacement for wall-time budgets; the user accepts the remaining traversal-attribution uncertainty for this change.

All six desktop disclosures pass (195.0–198.7 ms visual completion). The final uninstrumented mobile report passes five of six, but one shell disclosure records a 51 ms task despite 192.1 ms visual completion. The follow-up traced report reproduces four rejected 51–77 ms rendering tasks, with layout reaching 44.339 ms. These remain measured budget exceptions. The user accepted them explicitly and authorized synchronization and archival; no slow result has been discarded or relabeled as a numerical pass.

Verification logs: contain-retained-{unit,check,build}.log, contain-closure-{helpers,unused,bundles}.log, and contain-closure-<change>-validation.log. Unused-code findings remain non-blocking and unrelated. Repository-wide strict validation still has the separately documented baseline Purpose warnings and unrelated receiving-sink scenario omission; the four affected changes pass individually.

## Rejected containment experiment — 2026-09-05

- Exact implementation commit `6ce07d20` extended native containment to the first chunk of every later Distro group. Raw bundle, mobile-load, Store-activation, wide-scroll, mobile-scroll, and legacy-scroll output is ignored under `.codex-artifacts/runtime-performance/6ce07d20/`.
- Five cold mobile runs passed the load gates: Store All median LCP was 0.800 seconds with maximum CLS 0.0101; Store Distro median LCP was 0.928 seconds with maximum CLS 0.0007.
- Three desktop Store activations each settled all 104 cards with exactly one listing-price projection, zero per-card Store Offer reads, and zero Store 5xx responses. The local static projection remained the expected separately classified `404`.
- The experiment failed every Distro first-traversal profile. Wide runs produced repeatable 277–285 millisecond tasks and 286–292 millisecond long animation frames. Mobile runs produced repeatable 278–289 millisecond tasks and 287–299 millisecond long animation frames. Legacy runs produced repeatable 283–284 millisecond tasks and 292–295 millisecond long animation frames.
- Store traversal and Distro repeat traversal stayed below the application-work threshold. The rejected selector shifted deferred layout into the first Distro traversal instead of removing it; this is the responsible measured work.
- Decision: restore the previous single selector and its source-contract test, skip Browser acceptance for rejected code, keep this change and `right-size-store-coverflow-images` active, and leave production-readiness performance tasks open. No JavaScript loading controller, observer, pagination, virtualization, batching, node recycling, or dependency was added.
- Restored-tree commit `54129c86` passed the focused containment test, `pnpm test:unit`, `pnpm check`, `pnpm build`, unused-code and commerce-boundary audits, bundle-graph validation, and strict validation of every affected active change. Repository-wide `validate --all --strict` remains non-zero only because 17 pre-existing baseline capabilities still have generated placeholder Purpose text; the separate stale `soften-homepage-hero-exit` archive-readiness info also remains outside this child.

## Mode-aware remeasurement and rejected eager catalog — 2026-09-10

The retained implementation changes only `scripts/measure-runtime-performance.ts`, its existing helper, and helper tests. The production CSS experiment and its test were restored; the pre-existing reduced-motion CSS/test edits remain untouched. This is not a completed performance fix, a spec synchronization, or an archive decision.

### Measurement changes

- Each report records the Git revision, source/build SHA-256 fingerprints, relevant dirty-file hashes, browser version, build directory/mode, Product Environment, URL, cache policy, and profile. Input changes during a run invalidate its evidence.
- Traversal asserts controller readiness and records actual group modes, chunk display/containment, fonts, images, resources, and scroll extent. Full listing/Distro counts are separate from Coverflow enrollment: Distro has 101 cards, including one singleton outside its four enrolled groups; Store All has 104.
- `--traversal-mode=catalog` opens one group through its real View all control; `--catalog-group=2` selects the later CD group. Disclosure handler time, completion time, scroll position, tasks, and long animation frames are recorded separately. Opening all groups before tracing would warm later scroll corridors.
- First/repeat traversals retain raw Chromium traces and individual task maxima. Observer entries are timestamped and filtered by overlap with the actual scroll interval. The original wide reports predate that filtering; their isolated 70.5 ms observer frame without a corresponding long trace slice is not attributed to application traversal.
- Seven focused helper tests pass, including rejection of incomplete readiness and preservation of a 265 ms layout slice when windowed work p95 is below 8 ms.

### Results

Raw reports and traces are ignored under `.codex-artifacts/runtime-performance/contain-revised/`. Each baseline profile used three fresh-context runs per route/state; candidate wide and mobile catalog profiles also used three runs. Reports identify Chromium `151.0.7922.34` and the exact dirty working tree. Baseline and restored product output are separate from the rejected eager output; no evidence is labeled as a clean implementation commit.

| Distro profile | Native catalog maximum layout per run | Eager catalog disclosure handler per run |
| -------------- | ------------------------------------- | ---------------------------------------- |
| Wide, 4x CPU   | 140–160 ms                            | 840–917 ms                               |
| Mobile, 4x CPU | 124–125 ms                            | 956–1,009 ms                             |
| Legacy, 4x CPU | 121–125 ms                            | Not repeated after rejection             |

Native catalog first-traversal work p95 reaches 16.667 ms, above the 8 ms gate. Preview is inexpensive after readiness; expanded catalog is the reproducible failing state. Raw layout events identify document-root layout during chunk activation. The mobile trace additionally records a late Inter font load followed by a 104.557 ms layout (`baseline-mobile-catalog.json.store-distro-1-first.trace.json`). Font status alone was insufficient to detect this later glyph/font demand.

The tested CSS candidate set `content-visibility: visible` and removed intrinsic sizing only for existing catalog-mode Distro chunks. Its focused test failed before the rule and passed afterward. It removed the large traversal layouts, but synchronous disclosure tasks reached 841–918 ms wide and 957–1,010 ms mobile. Total disclosure took 1.15–1.27 seconds wide and 1.35–1.49 seconds mobile. This moves the stall into the click handler and is rejected. Additional candidate load, legacy, and full Browser acceptance runs cannot rescue that interaction failure and were not performed.

Decision: retain the existing renderer and the diagnostic tooling; leave performance acceptance open. No new containment selector, observer/activation controller, renderer, pagination, virtualization, image-loading policy, or font policy is retained. Any next implementation must address the measured disclosure/layout and late-font work under a specifically revised scope; this result does not authorize a passing performance claim.

### Verification scope

The restored tree passed `pnpm test:unit`, `pnpm check`, `pnpm build`, the seven focused helper tests, commerce-boundary validation, and bundle validation. `pnpm audit:unused` completed with non-blocking findings outside the changed runner files (three unlisted binaries, twelve exports, two exported types, and one duplicate export). Strict validation passed this child, production readiness, Coverflow image performance, and Distro disclosure changes.

Native Browser Use recovered after its initial connection timeout. Focused smoke checks on production output at 1440x900 and 390x844 confirmed later CD selection/disclosure, search matching/empty/clear states, all 101 Distro cards, all 104 Store cards, no horizontal overflow, and shell Store All/Distro navigation resetting focus to `MAIN` and scroll to zero. Console warnings/errors were absent in that pass. This is not the full overlay/player/reduced-motion/fallback accessibility acceptance matrix, which remains unchecked.

The local server used the installed Vite preview CLI to serve `apps/web/dist` at the normal base URL after Astro preview's background process exited before readiness. These are Local production-build diagnostics, not hosted UAT or PRD acceptance.

### Fresh restored-tree load and request results

`restored-mobile-load.json` contains five cold mobile runs per route: Store All LCP median/p75 is 0.816/0.836 seconds, maximum CLS 0.010066; Store Distro is 0.964/0.980 seconds, maximum CLS 0.000710. These lab load gates pass. `restored-activation.json` accepts three of three activations, each with 104 Store cards, one listing-price projection, zero per-card Store Offer reads, and no Store 5xx. The static-only listing-price `404` remains explicitly classified as API unavailability, not a hosted commerce result.

Both reports identify revision `d994bb01264f473cfc31bf69be78f9d5e99e4a33` plus source fingerprint `dcaf7254ac79946662e181838a7a5ff4f5074c41cdf37cd6effaf9a0ce5e6c41` and build fingerprint `77c6ee24bef998cb6590c65b73792496b38f704e60bb1bf965e4778a42383bfe`; inputs remained unchanged during each run. This includes the pre-existing reduced-motion changes and does not label the dirty tree as a new commit.

`restored-later-group-diagnostic.json` verifies selected group index 2 enters catalog mode, other group modes stay recorded separately, all 101 Distro cards remain present, and first/repeat traces are produced. Disclosure itself scrolls to 3,088 px and takes a 528 ms handler in this one diagnostic run. Because disclosure already visited that position, this is later-group setup coverage, not an untouched whole-page first-traversal acceptance run.

Fresh repository-wide strict validation reports 39 passed and 19 failed: 18 baseline capabilities have placeholder Purpose text, and the unrelated `adopt-resend-receiving-uat-sink` change omits the existing `Deployment ownership is validated` scenario from its modified tooling requirement. The four directly affected changes validate individually. Raw validation output is `.codex-artifacts/contain-openspec-all.log`; none of these unrelated documents was repaired in this slice.

## Reopened closure investigation

The user authorized the work needed to close all 23 tasks. Fresh single-run diagnostics in `.codex-artifacts/runtime-performance/contain-revised/` rejected explicit grid widths (`grid-mobile-catalog.json`), outer flex stacks (`flex-mobile-catalog.json`), unbalanced titles (`wrap-mobile-catalog.json`), a flex card surface (`surface-mobile-catalog.json`), chunk inline-size containment (`inline-mobile-catalog.json`), and removal of nested percentage card heights (`height-mobile-catalog.json`). Each still contained a layout above 50 ms. Those individual changes were restored before moving to the next diagnostic.

A font-only control (`font-mobile-catalog.json`) reduced the disclosure handler to 42.5 ms. Raw traces separated remaining 28–34 ms chunk activation from a 121 ms layout immediately following an Inter font response. Eager chunks plus the existing UI title font, explicit outer grid columns, and optional Google font swapping removed traversal layouts (`eager-font-mobile-catalog.json`, largest task 6.266 ms), but still failed disclosure: the separate disclosure profile reported 85/101 ms tasks. Adding stretch-based card sizing lowered the first disclosure handler to 87.1 ms (`combined-mobile-catalog.json`) and retained layout-free traversal, but is not accepted yet. All reports retain rejected timings; none of these diagnostics closes a task.

The first broader matrix (`contain-final/wide-catalog.json`) exposed previously unmeasured Store All expanded-catalog failures: 190–208 ms layouts across three first traversals. Default Store All preview is not evidence for View all 104. The same bounded remedy is now being evaluated on Store listing cards. Prior selected-tree cold loads passed (Store median/p75 856/876 ms; Distro 1,104/1,120 ms; maximum CLS 0.010066), but those results precede the Store All extension.

The existing runner used performance.now after a resolved animation-frame promise as its cadence clock. Wide callback-clock reports include a low-work Store repeat p95 of 19.2 ms and later low-work Distro p95 of 24.9 ms. The revised runner records native animation-frame timestamps and preserves callback intervals separately, while retaining every raw slice/task and a final post-scroll frame. These clocks differ by definition ([MDN requestAnimationFrame](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame)); no before/after frame-cadence improvement is claimed across that measurement correction. The original reports and their slow samples remain available.

The scoped-preview diagnostic (scoped-mobile.json) removes preview-only geometry from non-positioned cards. Both routes' first traversals have zero layout slices. One Store repeat raw RunTask spans 163.060 ms but contains no script, style, layout, or paint event; the browser records no overlapping Long Task or Long Animation Frame. It remains an unattributed raw scheduler span, not evidence of an application rendering stall or a deleted slow sample. Final acceptance still requires the complete matrix.

The disclosure runner now preserves timestamped Long Task and Long Animation Frame entries overlapping the measured click-to-reveal interval. Clearing duration arrays alone could admit delayed observer delivery from prior collapse/setup work. The six-run preliminary mobile report retains its one 51 ms rejection; the corrected final report must independently satisfy the same budget.

## Retained candidate and browser verification

Timestamped disclosure measurements confirmed real residual layout work. The scoped-preview candidate rejected four of six ready mobile disclosures (52–81 ms tasks); layout containment alone still rejected one of six (55 ms), and deferring focus until after a complete rendering frame still rejected one of six (59 ms). Adding inline-size containment to the same card boundary passed all six direct/shell mobile disclosures in `contain-revised/inline-card-disclosure.json`: handlers 21.0–32.2 ms, visual completion 189.3–198.6 ms, and no 50 ms task. These rejected reports remain retained. A separate browser-setup failure is preserved in `contain-card-layout-final-disclosure.log`; it is not counted as a pass.

The final implementation uses `contain: layout inline-size` on existing catalog cards, never block-size/paint containment or intrinsic-height estimates. Disclosure applies expanded state immediately, lets a complete frame render, then focuses and scrolls the selected card behind the existing cancellation token. The updated source contract was red without inline-size containment and green after restoration. The full unit suite, `pnpm check`, and `pnpm build` passed in `contain-retained-{unit,check,build}.log`; final build completed at 13:54 local time on 2026-09-10.

Native Browser Use verified direct and shell-managed Store All/Distro routes at desktop and mobile, all 104/101 canonical cards, expanded 104/55/39-card collections, search matching/empty/clear states, mobile CD format selection, Next and keyboard disclosure, selected-card focus, settled shell focus on MAIN and scroll zero, and no horizontal overflow or failed visible cover. The native accessibility tree exposes the expected headings, named search, disclosure state, and catalog links. Keyboard disclosure produced visible focus. Native `window.find` selected the final CD title, Analekta, and scrolled it into view; expanded CD height remained 25,900 px. Store All height remained 78,368 px through an end-of-document traversal. These heights describe the measured mobile viewport, not hardcoded layout estimates.

Reduced-motion checks retained all 104/101 cards. JavaScript-disabled reloads had no ready controller, all canonical cards, no horizontal overflow, and stable complete-page height (65,643 px Distro and 78,324 px Store All in that viewport). Temporary media/script/viewport overrides were restored. Release and nested artist overlays opened and closed correctly. The in-app browser blocked external embeds with `ERR_BLOCKED_BY_CLIENT`; native Chrome then loaded the real Bandcamp frame, accepted play intent, showed elapsed playback, minimized into the mobile Player Ready pill, and preserved one iframe across Store → Distro navigation. A final Stop check waited for iframe detachment and confirmed zero remaining iframes. Application console warnings/errors were absent. No provider mock or application behavior change was used for this verification.

Final-tree evidence is recorded in the current-result section above. Browser and repository checks pass; the disclosure and attribution residuals are accepted exceptions recorded in the current-result section.
