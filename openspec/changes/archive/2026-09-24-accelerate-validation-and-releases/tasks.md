# Tasks

Implementation handoff: **gpt-5.6-luna / max**, `$openspec-apply-change accelerate-validation-and-releases`. Read the proposal, design (including prototype evidence), and both spec deltas. Productize the selected implementation; do not repeat a concurrency sweep or add mechanisms beyond the now-proven CI follow-up. The ignored prototypes are executable sketches, not completed implementation.

Each of these ten tasks includes its verification. Work in order and keep a short result/evidence location in `performance-report.md`. Do not edit source/reports/checkboxes during timed validation. Preserve the normal checkout policy. Missing historical/hosted samples block performance acceptance, not independent local work; no benchmark-only releases or PRD promotions.

## 1. Preserve the measurement contract

- [ ] 1.1 Extend the existing benchmark minimally for aggregate-versus-aggregate and same-checkout capture/compare, retaining legacy use. Check invalid/partial/changed-source/incompatible/overwritten evidence rejection. Freeze source/tool/gate/assertion inventories and three comparable warm baselines plus fresh/fast diagnostic baselines, reusing retained controls only under design decision 1. Reuse the saved CI history/prototypes/provider correlation and add only the bounded read-only collector needed for repeatability; verify attempt/target/failure classification with fixtures. Record historical comparability gaps and the zero-extra-release measurement budget from decision 1.

## 2. Productize the proven test setup

- [x] 2.1 Move exactly the 21 proven backend files through one explicit shared Node selection; retain all Worker integration/SDK cases and default unclassified files to Workers. Apply the bounded Local-three/otherwise-two Worker preset and validated override from decision 3. Verify disjoint/exhaustive collection, unchanged assertion identities, effective caps, and default handling of a new unclassified file.
- [x] 2.2 Implement the tested web lightweight/request split with distinct reporter files and both package test aliases covering both groups. Preserve MSW lifecycle, lightweight fetch/HTTP/HTTPS rejection, and cleanup. Port the three network-rejection checks and verify all 109 existing web files / 566 assertions still execute once; retain root contracts and scoped command behavior.

## 3. Reuse formatting work safely

- [x] 3.1 Add the small native Prettier content-cache wrapper, complete identity key, ignored cache location, and uncached escape hatch from decision 4. Verify unchanged input coverage, cached/uncached parity, same-timestamp edits, config/ignore/plugin/tool invalidation, corrupt cache handling, and a formatting failure after warming. Keep lint uncached and add only this cache to safe fresh cleanup.

## 4. Shorten independent preparation

- [x] 4.1 Add partial `validate:checks` with explicit phase membership, then wire complete CI prerequisites and the selected web/staff and Chromium/Firefox overlap using existing process helpers. Preserve target ordering, post-build checks, unused audit, failure diagnostics, and every release boundary. Extend existing validator/process/workflow contracts for status/flag separation, source invalidation, child failure/cancellation, isolated outputs, and prerequisite joins; retain serial diagnosis and unchanged downstream jobs/credentials/locks. Integrate the narrowly scoped native image cache from decision 7, verify changed-image/transformation invalidation and safe miss behavior, and remove pnpm/install/store-cache setup only from the built-in-only inspection job. Record cache bytes and all restore/save overhead.
- [x] 4.2 Productize the four-wide immutable-media loop from the prototype. Extend restore tests for unchanged auth/target/deduplication/request/hash/byte behavior, fixed maximum concurrency, settled failed batches, no later batch/retry, and no accepted output on failure. Preserve sequential target restores and Free-tier requirements; carry the executable local fixture as evidence without claiming hosted savings.

- [x] 4.3 Fix only the paid-state wait predicate in `waitForRemoteOrderAfterCheckout` using `poll-candidate.ts`. Port the fake-clock ready/delayed/never-paid/non-paid regression and preserve final surface/projection rejection, timeout, D1 enrichment/fallback and two-scenario cap. Record first-paid timing/poll count in existing evidence; no provider replay or extra payment.
- [x] 4.4 Add compact pack/materialize operations to the existing release-candidate helper and wire every candidate consumer, including PRD. Reuse schema-2 manifest hashes, regular copies and native artifact ZIP compression level 1. Prove full-file round-trip and legacy compatibility, reject missing/corrupt/extra objects, path escape, duplicate destinations and symlinks, and preserve all authority checks. Reuse the real-image prototype, verify at least 50% payload reduction on the representative bundle, and record actual pack/restore/verify overhead.

## 5. Verify the actual implementation

- [ ] 5.1 Update command and release guidance; run final uncached formatting, validator/process acceptance tests, editor and affected publication/build checks. Capture three comparable final warm full runs and fresh/fast diagnostic pairs, verify complete inventories and at least **20% warm-median improvement** with design non-regression limits, and record exact source identities. Final ordinary `pnpm validate` must match the final tree; do not substitute prototype wrappers or repeat equivalent full suites solely for ceremony.
- [ ] 5.2 Use at most one optional 60-second shadow round-trip in an already-authorized normal Linux candidate job; no duplicate build/upload/deploy. After normal releases with evidenced Free-tier headroom, collect five comparable successful post-change UAT attempts and all conclusions, compare with baseline, and report actual execution/tail/job-second/request changes plus PRD controls, image-cache net saving and compact-transfer total cost. Keep image persistence only if it clears the 30-second net-saving rule; do not claim that the 92.5-second within-job compute difference is a measured cross-run gain. Finalize `performance-report.md` with accepted changes, evidence gaps, and rollback; run strict OpenSpec validation. Keep this task unchecked until hosted acceptance passes; no unmeasured percentage claims or manufactured release samples.
