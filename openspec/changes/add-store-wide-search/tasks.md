## 1. Confirm shared search and lifecycle ownership

Acceptance evidence: [validation.md](validation.md).

- [x] 1.1 Run `pnpm openspec:guard` and trace current Distro search, flat All coverflow initialization, lazy portal wiring and snapshot cleanup; record the single-controller ownership change and verify the existing module entrypoint/dependencies suffice.
- [x] 1.2 Complete the applicable Impeccable review for the accepted compact All discovery direction and Search Store using existing styling; review first-viewport artwork, shortened introduction, compact format handoff, and a single mode-appropriate count while preserving category navigation and policy links.

## 2. Extend the existing search implementation

- [x] 2.1 Add escaped All-card search metadata and an All search placeholder using existing item fields; verify all rendered items, off-preview entries, and absent optional metadata are represented without duplicate cards or new content fields.
- [x] 2.2 Extend `StoreDistroSearch.tsx` with shared All/Distro scope while reusing its control, matcher, feedback and cleanup; verify exact-match precedence, fuzzy fallback, canonical order, zero matches, whitespace/clear and one-item/empty catalog fixtures for both DOM shapes.
- [x] 2.3 Integrate All search with the existing coverflow controller and remove competing All initialization for the searchable shelf; verify at most one controller/handler set, independent ordinary-grid search, first disclosure retained across delayed readiness, complete-grid fallback on failure, all matching cards visible, clear-to-catalog with focus returned to the input, and unchanged untouched-preview behavior.
- [x] 2.4 Extend route-lazy mount/prefetch and snapshot sanitation for `/store/`; verify All-to-Distro-to-All, rapid cancellation, back/forward, clean query reset, Distro fragment/format navigation, and no search control on other categories.
- [x] 2.5 Compact All orientation and its format handoff, place search beside the shelf, and consolidate preview/catalog/search counts; verify no repeated metric blocks, unchanged canonical cards and Coverflow behavior, accessible format links, and no extra price reads or eager offstage images.

## 3. Verify reuse and performance

- [x] 3.1 Use native Browser Use for desktop/mobile, keyboard, reduced motion, no-results/clear, repeat navigation and script-disabled/failure fallback; record evidence that full static catalog access and Distro behavior remain intact.
- [x] 3.2 Measure the existing All/Distro load and mobile-stress profiles and inspect network activity; verify LCP/CLS/search-task budgets, no search network calls, one listing-price projection per activation, and zero per-card Store Offer reads.
- [x] 3.3 Run `pnpm test:unit`, `pnpm check`, `pnpm build`, and `pnpm performance:bundles` on the final tree; verify existing Distro/coverflow regressions pass and no dependency, remote index, or second matching implementation was added.
- [x] 3.4 Run `pnpm openspec -- validate add-store-wide-search --type change --strict` and `git diff --check`; record acceptance and confirm the Distro lifecycle, All catalog, and performance deltas preserve original scenarios while replacing the obsolete All-search prohibition before proposing archive.
- [x] 3.5 Capture initial All views at 390x844 and 1280x800 CSS pixels with default text size and scrollY=0, including the UAT review marker; verify identifiable first-item artwork is visible. Check 320px, 200% text, and 400% zoom reflow without clipping or shrinking text, allowing natural scrolling; verify 44px format targets and count feedback in all three modes.
