# Tasks

Implementation handoff for Luna-6-max. Read [proposal.md](proposal.md), [design.md](design.md), [the delta spec](specs/staff-workspace/spec.md), and [measurements.md](measurements.md), then follow these tasks in order. The design includes the second measurement pass and conditional estimates: roughly 50–200 ms saved on ordinary warmed visits, larger gains on CSS stalls, and no guarantee for slow HTML/API first visits. This is a new follow-up to `reduce-staff-navigation-wait` task 5.5; do not redo its completed implementation. The expected implementation has four edit points and no new dependency. Complete local work before any rollout decision. This planning request does not authorize a deployment.

## 1. Confirm the starting point

- [x] 1.1 Run `pnpm openspec:guard` on main, inspect the current diff and source SHA, and audit `packages/content-model/src/index.ts` plus its exports for required external import-time side effects. Record the SHA and audit result in this change's new `implementation-evidence.md`; preserve existing user changes.
- [x] 1.2 Retain a baseline staff build under an ignored evidence directory before changing configuration. Use `pnpm build:staff` when a matching standard build is not already available. Record its source/environment and Overview, Website, Stock, and Orders documents so task 3.2 can demonstrate the regression check fails on the old behavior. Do not depend on the planning experiment's copied package or Vite alias.

## 2. Separate runtime code and apply native settings

- [x] 2.1 Add `"sideEffects": false` to `packages/content-model/package.json` after the audit. Keep its root exports and consumer imports intact. Verify the resulting staff build drops unrelated content schemas from Overview/Orders while required Website/Stock validation remains; retain the before/after eager-size evidence, and use the full functional checks in section 4 to verify behavior.
- [x] 2.2 Set `build: { inlineStylesheets: 'always' }` in `apps/staff/astro.config.mjs`. Inspect built route documents to verify zero external initial project stylesheet links and compressed HTML below 24 KiB; retain Google Fonts and the existing lazy editor/picker style boundary. Task 4.2 verifies rendered behavior.
- [x] 2.3 Separate browser prose helpers and the safe-link predicate from Zod schema construction in an internal content-model module. Keep the root entrypoint, current consumers, trust-boundary schemas, and rich-text behavior intact. Add regression coverage for helper/schema parity on safe, unsafe, malformed, whitespace/backslash, and non-string link values, plus invalid-link rendering.

## 3. Guard the actual build result

- [x] 3.1 Extend `scripts/check-runtime-bundle-graphs.ts` with the staff profile from design decision 3, preserving the default web mode and existing CLI options. Reuse its eager-graph traversal and Brotli calculation. Verify staff output contains all four route reports, the exact JS/HTML budgets, stylesheet URLs, and actionable diagnostics; unknown scope, absent files, and exceeded budgets must exit nonzero. Do not add a second graph checker.
- [x] 3.2 Append `pnpm performance:bundles --scope=staff` to root `build:staff` after route isolation. Verify the retained baseline fails for external CSS and Overview/Orders bytes, the changed standard build passes, and the existing default web check still passes on its matching fresh build. Retain commands, exit codes, and compact reports in `implementation-evidence.md`; do not change budgets merely to obtain a pass.
- [x] 3.3 Append the existing default `pnpm performance:bundles` check to root `build:web` after Astro build and route isolation. Keep the 97,280-byte Home budget unchanged. Verify a fresh web build passes and the staff build still passes all staff budgets; retain both standard reports.

## 4. Verify consumers and staff behavior

- [x] 4.1 Re-run the canonical `pnpm --filter @blackbox/backend build:cms` and applicable CMS/content/publication checks after the shared content-model change. Retain its source/generated no-KV guard and no-session-cookie regression results. Verify Access, private 200/304 behavior, and validation at existing trust boundaries remain intact.
- [ ] 4.2 Run `pnpm validate` and `pnpm validate:editor` against the final implementation tree after source and documentation edits; inspect summaries and relevant failures only. Reuse existing Chromium/Firefox editor checks. Verify staff desktop/mobile behavior, direct editor entry, picker/history/preview styling, incomplete-draft validation, autosave, conflicts, save-before-leave, and recovery. Use the native browser interface to verify newsletter and purchase-information rendering, formatted links/lists/quotes, newsletter form behavior, and console cleanliness. Record the final passing source fingerprint; partial checks are not completion.
- [x] 4.3 Run `pnpm openspec -- validate reduce-staff-bootstrap-cost --type change --strict`, formatting, and diff checks against the final artifacts. Complete `implementation-evidence.md` with standard before/after web and staff build sizes, functional results, and remaining hosted work. Keep `measurements.md` as the unchanged September 24 baseline and leave hosted tasks unchecked until their evidence exists.

## 5. Assess an authorized hosted candidate

- [ ] 5.1 When UAT rollout is authorized, refresh and record the account-wide Free-tier worksheet, produce/deploy the normal reviewed canonical candidate, and smoke Overview, Website, Stock, and Distro once each. Verify initial project CSS requests are absent, styling and protected responses are correct, and the deployed SHA/version matches the candidate. Record this separately from local evidence.
- [ ] 5.2 After separate reviewed PRD promotion authorization and a fresh Free-tier preflight, perform the at-most-sixteen-activation protocol in the design using extension-connected Chrome. Record four first visits and twelve foreground repeat visits separately. Drain and save network events after each navigation, checking truncation; retain response Rays and connection timing along with resource/API timing, primary-content conditions, completed thumbnail bytes, usage accounting, and every slow or invalid sample. Keep the pilot within the reviewed ceilings; if capture or allowance fails, record the limitation and stop rather than rerunning indefinitely.
- [ ] 5.3 Compare the deployed candidate with the baseline, preserving network/cache/colo differences and all valid outliers. Require the built budgets and zero initial project CSS requests, correct behavior/security, complete evidence, and a repeat-visit primary-content median at most 1,500 ms per route. Report the actual improvement against the conditional estimate; do not declare all first visits fixed from this median.
- [ ] 5.4 For any first visit above two seconds, perform the design's bounded Ray/Worker/connection attribution using retained evidence and at most one three-GET control within the existing budget. Record which stage remains unexplained and its follow-up disposition. Keep the previous change's task 5.5 open while that startup issue remains unresolved; close it only with accepted evidence. Do not add provider, cache, auth, or region changes to make the frontend plan appear complete. Archive only after the normal completion review.
