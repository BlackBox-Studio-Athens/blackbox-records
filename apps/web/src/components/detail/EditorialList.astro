---
import type { List, Props as PortableProps } from 'astro-portabletext/types';
type Props = PortableProps<List>;
const first = Astro.props.node.children[0] as { listStart?: number } | undefined;
const start = first?.listStart ?? 1;
---

<ol start={start === 1 ? undefined : start}><slot /></ol>
