# Secret-free static builds stay disabled. cms:dev explicitly selects local mode.
SVELTIA_BACKEND_MODE=disabled

# Hosted mode requires the official Sveltia CMS Authenticator HTTPS origin.
# Set SVELTIA_AUTH_BASE_URL as a GitHub Actions variable after OAuth setup.
# OAuth credentials belong only in the authenticator Worker secrets.
