---
import EditorialBody from '../../src/cms/EditorialBody.astro';
import Prose from '../../../web/src/components/Prose.astro';

// Local test fixture only; never included in the production CMS root.
const value = await Astro.request.json();
---

{Astro.url.searchParams.has('prose') ? <Prose value={value} /> : <EditorialBody value={value} />}
