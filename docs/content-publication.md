# Content publication

The staff workspace saves private drafts through EmDash's native content API. Review changes finishes autosave and opens an individual review inside the editor without replacing the grouped selection. `/review/` supports Select → Review → Publish for up to twenty entries. Discovery compares saved revisions with accepted content because native collection lists retain published field values; revision reads remain bounded by each page's scan limit. Publication completeness is checked separately at every publication and snapshot boundary. Linked catalog entries retain Selling's explicit activation operation; editorial-only releases need no price or stock.

Authenticated `POST /_emdash/api/blackbox/publication-review` reads exact saved revisions and the accepted snapshot. It returns readable comparison values, completeness issues, required unpublished references, protected image sources and a baseline identity. Reviews and combined previews create no revisions or publication operations. Preview resolves the selected entries together against accepted content; it never substitutes an unrelated newer draft. Required unpublished artists are included only by explicit staff action. Missing or incomplete dependencies block publication.

Reviewed requests retain the baseline identity alongside revisions. Acceptance, preparation and activation check that identity; a changed website or draft requires fresh review. Legacy retained requests without a baseline remain recoverable. The final Publish change/Publish N changes action sends directly. Preparing, Checking website and Confirming publication reflect backend stages; On the website requires public confirmation. Visual preview failure offers Retry or explicit Publish without preview, which does not bypass content, dependency, revision or server rendering checks.

History uses the retained journal through cursor-paginated `GET /_emdash/api/blackbox/publications/history`, optionally filtered by collection and record ID. The staff menu opens one right-side history panel with status badges, the requested Athens time and expandable publisher, destination, entry and failure details. New intents retain entry titles/types, publisher, request time and destination. Missing legacy metadata is identified as unavailable. History has no restoration or undo actions; `Discard saved changes` is a separate editor action for the current unpublished draft and never restores an arbitrary historical revision.

A durable CMS journal precedes native publication. Preparation replaces selected entries in the last accepted snapshot, copies only new media, validates the affected public templates, and conditionally activates the immutable R2 manifest. Unrelated drafts never enter the candidate. Each entry's website state is compared with the accepted snapshot, not inferred from global publication history. Pending operations check every two seconds for one minute and every thirty seconds thereafter, stopping after thirty minutes with Update not confirmed and Check status. Hidden/offline views pause reads. Stock remains authoritative for physical copies, online quantities and movement history.

The public renderer runs Astro inside a SQLite Durable Object. Pages serves compiled assets and forwards public GET/HEAD requests through `PUBLIC_SITE`; staff cookies and authorization are not forwarded. The renderer has R2 access only, with no D1, commerce secrets, sessions or KV. Private namespaces are blocked. Snapshot media is publicly accessible only after acceptance. The pages.dev URLs, Local base path, checkout gates and holding branch remain unchanged.

The renderer checks the pointer at most once every five seconds per active object. Its bounded HTML cache keys include code and snapshot identity. HTML uses `no-store`; versioned media is immutable. A transient pointer-read failure keeps the last verified in-memory snapshot. Cold starts without readable accepted content return 503. The site does not fall back to drafts. Sixty seconds is the normal-service p95 publication target, not an outage guarantee.

## Recovery

The browser retains the request ID before sending it. Retrying an uncertain response returns the same request; conflicting reuse is rejected. The CMS arms its alarm before processing and resumes pending work after eviction. Retries back off from two seconds to a five-minute ceiling. Pre-activation failures retry up to six attempts and then preserve the previous public snapshot. Once activated, confirmation remains recoverable and cannot be reported as a preparation failure. Live requires the public origin's content identity to match. Investigate repeated service failures before manually retrying a failed request.

R2 manifests, media and the accepted pointer are durable. Code promotion consumes the reviewed paired renderer/CMS/gateway artifacts and keeps that pointer. Initial cutover bootstraps from the previously accepted snapshot identity. An older static deployment is not a content rollback: its content may be stale. Use a reviewed compatible code candidate; content rollback requires a separately selected, verified accepted snapshot.

## Local verification

`pnpm dev:stack:stripe-mock` waits for CMS initialization, imports only an empty library, and links only its initial fake catalog while preserving prices and stock. The public service stages the initial verified snapshot once. Restarts reuse the current R2 pointer and do not promote saved drafts. No Local operation dispatches GitHub or contacts hosted providers.

Run `node --import tsx scripts/test-local-content-publication.mjs` against that stack. It changes and restores two Local artist drafts, measures publication latency, retries the same request ID and checks unrelated-draft privacy. The runtime Vitest regression covers failed validation, lost confirmation, compare-and-swap and request conflicts. The renderer smoke under `apps/backend/test/emdash/public-runtime-smoke.mjs` checks the accepted snapshot's public routes and media against a separate Local store. Standard unit/check/build gates and both content-editor browser regressions remain required.

The Local publication smoke also checks combined preview, authenticated accepted-image delivery and review non-mutation. The September 2026 run measured 6.7 seconds for one entry and 6.5 seconds for two entries; review made two and four native CMS reads respectively. `X-Publication-Review-Reads` measures native content/revision/media lookups only, excluding authentication, snapshot R2 reads and media delivery. Each review additionally reads the accepted pointer and snapshot; new image lookups are deduplicated and capped at 100. This is Local evidence, not hosted billing evidence. Run `node --import tsx apps/backend/scripts/smoke-content-preview.mjs --browsers` for real public-template preview checks in Chromium and Firefox.

## Isolated Local preview checks

Parallel worktrees must use separate ports, persistence, and `WRANGLER_REGISTRY_PATH` directories. Wrangler's default registry is shared across worktrees; different listening ports alone do not isolate service bindings. Keep the canonical launcher on 8787/4321. The preview acceptance fixture uses CMS `127.0.0.1:8799`, its isolated iframe origin `localhost:8799`, and public renderer `127.0.0.1:4339`, with unique Worker names and private storage. Build its public bundle with `PUBLIC_BACKEND_BASE_URL=http://127.0.0.1:8799` so both pages read the same shopper data. Set the fixture CMS binding `LOCAL_PUBLIC_ORIGIN=http://127.0.0.1:4339/blackbox-records/` for publication confirmation; hosted environments ignore this Local override.

Both smoke scripts accept `PREVIEW_STAFF_ORIGIN` and `PREVIEW_PUBLIC_ORIGIN` for those alternate loopback ports. `smoke-content-preview.mjs --browsers` covers all thirteen collections, private routing, paired screenshots, layout, shell history, search, memory-only cart and player integration. Set `PREVIEW_BROWSER=chromium` or `firefox` to run sequentially; `--pairs-only` repeats the representative comparison and interaction checks. The comparison uses the real font files with `font-display: swap` in its browser fixture, waits for the public lazy controls, and matches viewport and pointer position. Production retains optional fonts and lazy loading. Screenshot reports under `.codex-artifacts/preview-parity/` are review evidence, not a pixel-percentage release gate.

For the related Artist/Release publication case, set `PREVIEW_FIXTURE_PERSIST_TO` to the disposable store under this worktree's `.codex-artifacts/`. Its `identity.json` must contain the bootstrapped Home record's `homeId`; the script checks it before any mutation. Prepare the fixture through the existing Local migrations, CMS import/publication and fake catalog linkage, including the canonical guard that preserves existing prices and stock. The test uploads an image, changes one fake Store Item identity, previews and publishes the related batch, compares image bytes and the Store URL, then restores the original drafts, public content and identity. Its disposable media and publication history remain in the isolated store. No hosted provider or canonical Local database is used.

## Free-tier operation budget

Owner: the existing BlackBox release workflow and CMS/public runtimes. Each environment adds one service-only public Worker and one SQLite Durable Object namespace. Existing R2 buckets and CMS D1 databases are reused. No paid plan, KV binding, queue or additional cron is introduced. CMS and public build wrappers reject KV in source and generated configuration.

Text-only publication normally uses one journal insert, a native revision transition, roughly ten journal reads/writes, one manifest write, one pointer write and one acceptance marker. Rendering validation is bounded to sixty-one distinct pages for a twenty-record batch. Confirmation and retries add bounded reads; image changes add one source read and immutable write per new image. History polling is read-only. Public HTML and media consume Worker/DO requests; pointer reads are limited by the five-second interval, while media costs one R2 object read per uncached delivery. The HTML cache is capped at 64 entries and 8 MiB. These are application operation estimates; D1 billed rows include indexes and queries.

Release pilot ceiling: one UAT text publication, at most 100 public requests, 100 R2 writes, 1,000 R2 reads, 5,000 D1 rows written, 100,000 D1 rows read, and 100 GB-s additional DO duration. Reserve at least half of each Free allowance for ordinary service. Stop the pilot on any quota warning, unexpected write amplification or failure loop. Exhaustion returns an explicit pending/failure or public 503; it never enables billing or changes the accepted pointer to an incomplete candidate. Record account-wide usage and measured results in the change's release evidence before hosted work.

Pages service bindings use the API's `deployment_configs.production.services` field ([Cloudflare API](https://developers.cloudflare.com/api/resources/pages/subresources/projects/methods/edit/)). The gateway binding is verified before upload. Deployments stay in `.github/workflows/pages.yml`; PRD consumes the accepted UAT candidate without rebuilding and does not authorize catalog mutation or checkout launch.
