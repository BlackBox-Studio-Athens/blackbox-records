# EmDash retirement inventory

Prepared against `9e87c5e97a1edff0eb890a9f848893b2491a9482`. This is the removal batch for after PRD replacement acceptance, not evidence that retirement has happened. Follow [the cutover worksheet](cms-cutover.md) first.

| Remove or replace together                                      | Current entrypoints                                                                                                                                                                                                                                                                                             | Retain                                                                                                                         |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Sveltia writer, bootstrap, previews and generated configuration | `apps/web/src/pages/admin/`, Sveltia/Decap modules under `apps/web/src/lib/admin/`, `apps/web/scripts/start-cms-dev.mjs`, `apps/web/scripts/check-sveltia-build-mode.ts`                                                                                                                                        | A read-only retired `/admin/` entry or link to the protected staff workspace; public content schemas and media                 |
| Sveltia build and acceptance hooks                              | `apps/web/package.json` CMS commands and build-mode check; `scripts/smoke-cms-local.ts`; Sveltia assertions in `scripts/smoke-uat-static.ts`; `SVELTIA_*` inputs in `scripts/start-local-stack.ts`, `scripts/release-candidate.mjs`, `.github/workflows/pages.yml` and `.github/workflows/prd-holding-page.yml` | Public route, cache, image and metadata checks; one normal Local stack command                                                 |
| Compiled catalog authority and routine generation               | Root `postinstall`, `build`, `check`, `test:unit` generation hooks; `scripts/generate-stripe-uat-catalog-artifacts.ts`; generated manifest imports in `apps/backend/src/application/commerce/catalog-sync/{index,desired-catalog-state,catalog-product-projections}.ts`                                         | Runtime D1 catalog, provider mappings, stock ledger, orders, explicit import/recovery tooling and Local mock fixtures          |
| Separate hosted staff deployment                                | Staff Pages upload in `.github/workflows/pages.yml`; retained `prd/staff` bundle contract                                                                                                                                                                                                                       | `apps/staff` source and assets consumed by the combined CMS build; provider project history until recovery no longer needs it  |
| Legacy PRD Worker artifact selection                            | PRD promotion currently uploads `prd/worker/index.js` using the commerce Wrangler configuration                                                                                                                                                                                                                 | Retained and verified `prd/cms` combined artifact, exact Access protection, target mutation lock and reviewed release identity |

The catalog modules have callers beyond their generated import: move those callers to runtime catalog seams in the same batch before deleting exports. Keep schema migrations and historical order references intact. Do not delete generated Prisma models merely because their names contain “catalog”.

Remove Git OAuth integration only after identifying its hosted application and consumers; the repository's Sveltia auth URL alone does not prove that a provider application is unused. Do not delete a provider project as a substitute for removing its active route or deployment step.

Acceptance for the batch: ordinary install, checks, build, runtime and publication work without compiled catalog generation; old admin routes cannot write; protected staff HTML, assets and APIs use the combined Worker; fresh public content comes from the accepted target snapshot. Reconcile module ownership and final OpenSpec deltas after these changes, and preserve the PRD launch gates.
